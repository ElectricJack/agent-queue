# Narrow11d completion amendment — per-project cadence

Controller inspection found an original-spec operational gap: design lines298–299 requires configurable per-project interval, default300seconds; scheduler.configure exists but only tests call it. Close this through the existing generation-CAS control, not a new command or database schema.

1. Add optional strict positive integer `interval_seconds` to `IntegrationEnableArgs` and `IntegrationControlService.enable`, forwarding it from `_cmd_integration_enable`. It is valid only with `mode=train`; reject other combinations before writes. Keep existing LOCAL-only authority and expected-generation checks. Do not implicitly cancel an active drain to change cadence: when an interval is supplied during draining, return a structured blocked result with no changes.
2. Extend `_configure_schedule_on` to accept the optional interval inside the SAME hierarchy-locked cutover transaction. A new schedule uses supplied interval or existing300default. Existing schedule preserves interval when omitted; changed interval recomputes next_due_at = now + interval_seconds deterministically. Preserve outstanding request identity/sequence/manual request, leases, active batch, and all coalescing/one-train semantics; no additional train starts. Do not call public scheduler.configure in a nested transaction. Same-mode train update still advances generation and existing transition audit with operator reason.
3. Expose `aq integration enable PROJECT --mode train --interval-seconds N --expected-generation G --reason R`. No interval auto-retry. Show effective interval via existing status.schedule; document default300, configurable override and cadence-change example. CLI rejects invalid positive integer/mode combinations clearly, server independently validates direct callers.
4. Focused tests for default/preserve/override, deterministic next due, pending-request preservation, stale zero-write, draining zero-write, non-train/zero/negative rejection and CLI transport. Use real control service/generic adapter where appropriate. No broad recovery/race matrix. Existing status read-only and observe behavior must stay intact.

## Command inventory test compatibility

Current combined11d gate145pass2fail identified tests/test_command_surface.py only inspecting legacy `_ALL_TOOL_DEFINITIONS`, omitting integration's real typed contract registry. Correct inventory to include actual contract-projected definitions from the shipped public discovery path; preserve orphan/uniqueness/schema and empty-schema ratchets. Do not whitelist every integration name as debt or relax the assertions. Test this file once after correction, alongside amended focused cadence tests; don't repeat already-green145gate merely for the inventory correction.

This amendment is within the requested operational completion and original spec, not a new feature. No security certification, production mutation, new migration or new command family. Append exact evidence to11d report and include changed backend files in scoped11d review.

# SDD ledger — plan: docs/superpowers/plans/2026-09-04-hierarchical-integration-trains-implementation.md

User authorized implementation in this workspace with Sol/Terra subagents and review between phases.
Workspace: /home/jkern/dev/agent-queue2; branch feat/hierarchical-integration-trains; initial base e6dc14a6.
Unrelated untracked dashboard-performance-investigation spec must remain untouched.

## Current checkpoint (2026-09-05)

Tasks 1–6 and Task7a/7b complete and independently reviewed. Task7c implementation is
complete at 9cad1d2e and under independent review. Overall Task7 awaits that verdict.
Task7a/7b/7c and Task8 preflight rulings are in their briefs. Historical entries below are retained audit,
not current status. No external push/PR/merge/enablement authorized.

Ruling: Execute Task 8 as two sequential independently reviewed Sol phases: 8a owns the
durable schedule plus schema invariants, and 8b owns conn-owned eligibility plus atomic
sealing — this makes the migration/schedule contract stable before the concurrency-heavy
frontier transaction and gives each surface a bounded review — cost if wrong: one extra
review handoff and possible narrow unpublished migration amendment before 8b.

Task7c initial review: 0 Critical, 1 Important, 1 Minor. Important: failed-child block
compiled to ordinary failed lifecycle rather than the required distinct blocked terminal
run outcome. Fix round1/5 assigned to the original Sol implementer from 9cad1d2e; warning,
skip, and corrected bare-pytest history remain a carried process Minor.
Ruling: Add `blocked` as a first-class terminal Playbook V2 run lifecycle and authored
terminal outcome, including durable schema and public typed status, while leaving the
domain parent PAUSED with its actual failed-child blocker — a failed run cannot distinguish
intentional subtree policy suspension from engine failure — cost if wrong: a small V2
lifecycle migration/API compatibility surface can be collapsed back to failed later.

Task7c fix round1: original Important addressed; scoped review found one new Important:
blocked terminal step receipts fall through the engine classifier as success. Fix round2/5
assigned to the original Sol implementer from 7bbdaef2, limited to failure classification
and direct receipt regression evidence.

Task7c fix round2/5: blocked receipt classification finding addressed; no new Critical or
Important breakage. Task7c complete at 39040c75 after two fix rounds. Overall Task 7 is
complete: 7a, 7b and 7c each independently reviewed; the shipped hierarchy playbook remains
disabled and no external/operator action occurred. Task8a starts from BASE39040c75.

Task8a implemented at 77dc4ea7 with report 5fcb3238. Initial review: 0 Critical,
1 Important, 1 Minor. Important: old schema legally permits non-empty batches with null
base/branch, but the new identity CHECK was installed without reconstruction or an early
compatibility guard. Fix round1/5 assigned to original Sol implementer from 5fcb3238.
Ruling: Before any Task8a migration DDL or trigger drop, fail clearly if a legacy non-empty
batch has null base SHA or integration branch; do not guess Git identity or backfill invented
refs — no authoritative exact reconstruction is guaranteed by the old row — cost if wrong:
an operator must drain/reconcile those legacy rows before upgrade rather than receiving a
best-effort fabricated identity.

Task8a fix round1/5: legacy nullable-identity guard finding addressed on SQLite and
PostgreSQL; no new Critical or Important breakage. Task8a complete and independently
reviewed at 44018637. Carried Minor: existing dependency warning noise. Task8b atomic
full-frontier sealing starts from BASE44018637 with task-8b-brief.md.

Ruling: Make the canonical public task-status transition use `db.immediate()` so SQLite
acquires its writer lock before the pre-state read; PostgreSQL remains an ordinary
transaction plus the existing project advisory lock — a deferred SQLite transaction can
otherwise read before a concurrent seal commits and then miss the sealed-ancestor guard
during reopen — cost if wrong: SQLite task transitions serialize earlier at transaction
start, with focused general-transition and seal-race coverage required.

Task8b implemented at 96771800 with report 4548c151. Initial review: 0 Critical,
3 Important, 2 Minor. Important: nonempty seal clears active coalescing request; public
review append does not share the PostgreSQL hierarchy exclusion; generated branch embeds
unconstrained project ID. Controller reconciliation also found the explicit sealed fact
omits separate batch_id. Fix round1/5 assigned to original Sol implementer from 4548c151;
warning and test-file-size Minors are deferred.
Ruling: All append-only review-evidence writers must participate in the project hierarchy
transaction lock; the trusted producer already does so, and the public append seam must
derive the source task project and acquire the same lock before insert — otherwise a newer
rejection can become latest during a PostgreSQL seal — cost if wrong: project-local review
inserts wait behind sealing, without serializing unrelated projects.
Ruling: Construct integration refs only from deterministic hexadecimal digests of project
and request identity, never raw project text, and validate the complete result against the
existing conservative Git ref rules in tests — legal project IDs are not Git-ref-safe and
sealed branch identity cannot be repaired later — cost if wrong: operator-facing refs are
less readable, while batch/request audit preserves exact provenance.

Ruling: Extend the original parent conflict intent with immutable resolution_reserved state and a repair-session-only reservation followed by existing trusted reconciliation — preserves original failed inputs, reserved receipt and crash-safe ordering rather than conflating check accounting with Git proof — cost if wrong: localized intent migration/API rework.
Ruling: Parent conflict resolution accepts only a linear non-merge exact commit range from the original target, verified against the exact remote tip/tree — gives explicit recoverable repair provenance while keeping roll-forward semantics — cost if wrong: an agent must flatten a merge-shaped local repair before reserving, without changing tested content.
Ruling: Shipped policy needs no extra pre-wake resolution reviewer; proven receipt then aggregate parent verification and normal parent review suffice — design permits but does not mandate an additional resolution review, and user wants to avoid redundant runs — cost if wrong: add a stricter playbook gate later.
Ruling: Active parent repair delegates default emergent filings to their real operation parent; active batch repair delegates require explicit root placement, with transactional authority recheck and delegate quota/provenance — preserves recursive child completion without synthetic delegate hierarchy or late stage authority — cost if wrong: explicit placement UX adjustment.

Ruling: Use the current checkout and a new feature branch, not a new worktree — user explicitly requested this workspace — cost if wrong: branch can be renamed without moving work.
Ruling: Recommended repair defaults are 1800s/3 attempts primary and 3600s/3 attempts debug, configurable — implementation spec supplies these defaults — cost if wrong: project config changes.
Ruling: Source plan's sample tests are behavioral sketches; implement executable fixtures and enforce actual required schema fields — examples contain helper names rather than existing fixtures — cost if wrong: more test code, no policy change.

## Preflight task consistency

| Task | Internal consistency check | Resolution |
|---|---|---|
| 1 | Migration generation vs never touching operator DB | Generate only against explicit scratch configuration; no operator upgrades |
| 2 | Registration precedes future handlers | Register real handlers only as implemented; no success stubs |
| 3 | Explicit lease can technically force refs | Require ancestry before parent/main pushes |
| 4 | Receipts after push vs retry evidence | Prepared intent and retained OID precede push |
| 5 | Immutable origins vs pre-start reparent | Retire reservation, allocate new origin |
| 6 | Close transaction vs remote HEAD I/O | Fence ownership and snapshot external evidence before transaction |
| 7 | Parent primary repair vs root initial CI | Root deadline begins construction; parent begins first failure |
| 8 | One pending request vs many windows | One outstanding request per project; consume atomically |
| 9 | Sealed members vs rebuilt squashes | Mutable revision results separate from immutable manifest |
| 10 | Cleanup failure vs lease release | Delivery receipts durable before release; cleanup independently retryable |
| 11 | Enablement vs external writes | Implement controls locally; do not enable operator project |
| 12 | E2E reset vs scope | Only disposable scripted E2E infrastructure; never production |

## Shared files and interfaces preflight

| Producer/consumer tasks | Shared boundary | Check |
|---|---|---|
| 1 / 2–12 | schema/models and adapter composition | New persistent APIs, no runtime enablement |
| 2 / 4–11 | integration command mixin/contracts | Sequential ownership; register when functional |
| 2 / 7,8,10 | durable event acceptance | Outbox ack must follow durable acceptance |
| 3 / 4,5,6,7,9,10 | ownership and Git expected-OID push | Same repository/ref key and fence everywhere |
| 4 / 5,6,7,9,10 | prepare/push/reconcile receipts | Domain key, not activation key |
| 5 / 6,7,8 | hierarchy state and generation | Same project lock used for mutations and sealing |
| 6 / 7,8,11 | readiness/evidence | task completion distinct from delivery |
| 7 / 9,10 | repair and CI attempt identity | Shared operation/stage identities |
| 7 / 10 | vault and prose playbook install | Both disabled by default |
| 8 / 9,10,11 | batch lease/schedule | Only current revision promotable; no next active batch |
| 9 / 10,11,12 | attestation, candidates, controls | exact head plus producer/check set, no forge merge API |
| 10 / 11,12 | cleanup/service and enablement | inactive by default; status/control through handler |
| 11 / 12 | operator guide and final tests | record real checks and remaining limits |

## Progress

Ruling: Expose frozen on_failed_child in typed readiness output and branch in authored playbook: block ends the run while existing parent suspension/failed-child guard persists; ask uses existing deduplicated human gate for the parent — keeps requested flexible policy in playbooks rather than adding side effects to read-only readiness or falsely exhausting repair — cost if wrong: localized fixture/read projection adjustment; human resolution never waives delivery evidence.
Task7c seam checks: agent found missing ready→transfer inputs; controller permits exact typed target/token/verifier fact data but requested actual post-receipt mark_ready_on invocation path before accepting emitter-only fix. Readiness is read-only; branchless verifier allocation needs explicit mechanical preparation if no current path. Existing transfer-owner replay early-return can skip wake after crash; actual route test must cover. No automatic failed-child state mutation approved.

Task7c provenance binding follow-on GREEN: actual LiveCommandExecutor mismatch test now rejects complete ArtifactRef/playbook/hash differing from managed operation frozen route with PromotionInvariantError. No invocation retains explicit configured local/service path; context is provenance only. Count for final post-follow-on gate pending.

Task7c slice1 committed3f5139cd overBASE6d8f86f4. LiveCommandExecutor server-only immutable actual-StepContext snapshot around invoke, nested/exception/cancel restoration, PromotionService current run/step/attempt notparentancestry. RED4 missing-context failures thenprovenanceNone; GREEN full single test_command_executor32 passed, Ruffclean. Remaining frozen routing/eventschemas, offline disabled fixture/actual flow/full7reconciliation, phase review.

Task7b: fix round1/5 (4 addressed,0 open; commits46f70a46..6d8f86f4), scoped task-7b-rereview-1.md clean/no new breakage.
Task7b: complete (commits5eee4036..6d8f86f4, review clean). Runtime46f70a46, fix6d8f86f4; final affected383 pass/3skip and two-dialect guarded U-D-U2 pass; baseline warnings deferred. Auth caller-instance claim, explicit frozen-origin push and safe rollback guard are binding downstream. Task7c starts6d8f86f4, last7phase before overall7completion; no push/PR/enablement.

Task7b fixround1 implemented6d8f86f4, clean worktree. Final aq383 passed/3 skipped/14 baseline warnings, SQLite+uniquePG guarded U-D-U2 passed, changed-file Ruff passed. Controller read appended report incl exact commands, approved claim propagation, frozen explicit remote, stable competing-intent failure, safe pre-DDL downgrade guard. Package review-46f70a46..6d8f86f4.diff sent original reviewer, same four findings+fixdiff only; safe-downgrade Ruling explicitly supplied. Report remains intentionally ignored, no need repeat tests.

Task7b fixround1 guard/credential GREEN checkpoint: test_api_auth40 passed; focused principal+resolution1+1+4; promotion51 passed; repair34 passed; aq agent_terminals+session_commands+supervisor_global_lifecycle125 passed; SQLite+uniquePG U-D-U/live-drain guard2 passed. Claim preserves cache/reload/scope/principal; NULL accepted unrelated but rejects resolution/repair filing. Await final report exact commands, affected gate/lint/commit and scoped review; no blocker.

Task7b fixround1 checkpoint: frozen-origin and reverse-competing-intent focused GREEN2 passed. Seeded-live migration RED hits raw SQLite IntegrityError rather than approved explicit pre-DDL drain guard; implementing guard then server-issued instance claim. No blocker.

Ruling: Persist optional session_instance_token in server-issued API token rows/cache, carry unchanged through RequestScope/ExecutionPrincipal, supply it at five server mint sites, compare against final locked resolution/repair-filer identity; legacy NULL tokens retain unrelated behavior but cannot perform managed writer mutations — current-row tokens are not caller identity and cannot fence a replaced process — cost if wrong: narrow auth-schema/propagation compatibility rework and fresh token/restart at managed cutover; no caller-controlled identity input.
Ruling: Guard downgrade before any DDL while resolution_reserved rows exist, with explicit reconcile/drain precondition and unchanged-data tests, rather than dropping reservation evidence and relabeling conflict — remote mutation can have happened before receipt commit, so silent proof loss violates durable recovery — cost if wrong: operator must drain/reconcile before rollback; avoids unrecoverable intent loss. This supersedes review's suggested conversion remedy, not its accidental-constraint-failure finding.
Task7b fix scope approved: same instance-bound helper only for touched managed repair filing; orchestrator complete_delegate already carries actual internal SessionRecord token and remains unchanged unless concrete gap. Tests cover cache/reload/HTTP handler scope, original+successor and filer replacement, NULL claim, ordinary compatibility. No broad auth overhaul or client generation absent actual DTO/schema change.

Task7b initial review needs fixes: Critical caller instance is not authenticated (RequestScope/ExecutionPrincipal/token claims only sessionid; reads current row token), Important mutable workspace origin external target, live resolution_reserved downgrade state failure, reverse competing-intent uncaught IntegrityError. Original implementer fixround1 assigned46f70a46. Controller independently inspected principal.py/api/auth.py and mint call sites confirming immutable token instance claim absent, requested smallest persisted claim→scope→principal→final mutation comparison proposal before broad edits; exact launch instance exists before mint. Three bounded other fixes may proceed. Existing7a filing/close caller binding should be assessed for same shared gap. No runtime controller edits.
Task7b minor (deferred): dependency deprecation warnings remain in green runs; final review triage. Cannot-verify stop/detach seam resolved by reviewed7a actual stop instance-CAS and scheduler/retained launch tests (task-7a-rereview-1/2.md);7c routing/cutover explicitly later.

Task7b implemented46f70a46, clean worktree. Final affected aq137 passed/11 baseline warnings; SQLite+uniquePG U-D-U2 passed; Ruff/diff clean, single Alembic head8b4d2f7c1a90. Controller read full report/test evidence, package review-5eee4036..46f70a46.diff dispatched fresh /root/review_task7b_sol. Actual lifecycle event fields project_id,operation_id,promotion_intent_id (corrected7c brief from initial shorthand intent_id). No completion until review gate.

Ruling: Preserve the committed shared scope helper's operation/stage→owner→workspace/session lock order, then recheck the already-held owner via mutation_exclusion_on on the same conn; do not add resource→owner opt-in — controller inspected5eee4036 get_repair_filing_scope and corrected earlier overly-specific resource→owner wording, which would create inconsistent orders — cost if wrong: revisit narrowly factored push locking; correction avoids an unnecessary opt-in/refactor and retains final write authority.
Task7b final-gate checkpoint: SQLite+uniquePG migration2 passed; promotion file44 passed before final refinement. Controller corrected its earlier lock-order prescription as above before area gate; agent should preserve original shared ordering and remove only its own new opt-in if added. No production blocker.

Task7b crash/successor negatives GREEN: aq test promotion+ownership -k 'resolution_push_rejects or resolution_push_crash or resolution_push_rechecks or debug_successor or conn_owned_mutation' -x =>10 passed/9 warnings. Wrong tree/unlisted range/merge, moved target, before/after push crashes, pre-lock expiry/session replacement, conn-owned compatibility, actual primary→debug immutable-reservation recovery covered. Completion+rollover1 passed after correcting old test ArtifactRef generation. Remaining malformed-proof/session negatives, dialect cycles, design inventory/final area/Ruff/report/commit.

Task7b parent consumer GREEN: real resolution receipt→readiness→aggregate verify→guarded completion1 passed, exact operation/episode/verification completion pin asserted. RED was waiting instead of ready because old consumer required after_sha==squash_sha; separate strict resolution-proof consumer added without relaxing clean receipt shape. Remaining crash/stale/successor negatives/dialect/final gates.

Task7b push/reconcile core GREEN: focused real-Git1 passed. No-OID push uses conn-owned mutation exclusion; reconcile verifies exact remote tip/tree/linear range then original receipt/delivery/cleanup. Actual new lifecycle fact integration.resolution_push_observed carries operation_id+intent_id for7c. Remaining crash/stale/successor tests, parent consumer and dialect/final gates.

Task7b push lock-order seam approved: factor BranchOwnership.mutation_exclusion_on(conn,...) without changing existing wrapper behavior. Resolution reservation/push takes canonical operation/stage/task/resource locks then owner on same transaction, holding final stage/deadline/session-instance/workspace/fence authority through actual Git write; no nested connections or reversed owner→operation ordering. Add stage/session replacement interleaving and existing ownership compatibility tests. Supports existing single-writer requirement, not policy expansion.

Task7b reservation slice GREEN: same aq promotion-file selection for operation_key/reservation/session-only command =>3 passed, baseline deprecations. Original-intent resolution_reserved schema/query, operation_key immutable reuse, exact attached/unexpired SESSION authoring including instance token, typed resolve command implemented. Remaining guarded push/reconcile/receipt, parent consumer, crash/dialect/final gate.

Ruling: Add SESSION-only integration_push_conflict_resolution(intent_id,fence), with no caller OIDs, after the separate immutable reservation and before trusted reconciliation — existing delivery_promote is collector-only and raw shell push cannot enforce ownership mutation exclusion — cost if wrong: one narrow command/fixture surface to revise, no broader Git authority.
Ruling: Separate immutable resolution authoring provenance from current retry-writer authorization; an exact current debug successor may push the same pre-reserved OIDs after proven handoff, never change the reservation — otherwise expiry/crash between reservation and push permanently strands the original child intent despite the required debug recovery — cost if wrong: narrowly scoped recovery-authority/audit adjustment, immutable delivery inputs remain intact.
Task7b first RED: aq single promotion file selected operation_key/replay/reservation/command cases =>1 expected failure, operation_key mutation incorrectly accepted. Implementer proceeding reservation/schema+strict command. Approved initial reservation fields/outcomes plus exact session-instance provenance; push command proposal approved with successor-recovery test requirement.

Task7a: fix round2/5 (1 addressed,0 open; commits29d68bf4..5eee4036). task-7a-rereview-2.md approved with no new breakage; only carried dependency-warning minor,7c/9 seams remain later gates.
Task7a: complete (commits22a7f911..5eee4036, review clean). Commits0650d4e8,41723057,29d68bf4,62d4c0aa,5eee4036. Final broad affected gate330 pass/6skip before narrow dossier fix; final dossier13 pass plusclean3 pass. No enablement/external writes. Task7b startingBASE5eee4036 with standalone ordered brief+resolution preflight; overall Task7 remains incomplete.

Task7a round2 implemented62d4c0aa/report5eee4036. Exact server Git range proof outside DB transactions bound to subject/HEAD/session/fence CAS; late matching receipts refreshed at debug activation/retained dispatch; frozen manifest/budgets/replay preserved. Final focused13 passed/21 deselected, clean filing-close3 passed, real dossier1 passed,3 baseline warnings; Ruff/diffclean, no schema change. Controller read report commands/output and dispatched review-29d68bf4..5eee4036.diff to original reviewer for sole finding9 plus fixdiff breakage.

Task7a round2 dossier slice GREEN: single repair file -k debug_dossier_refreshes_exact -x =>1 passed/33 deselected. Real three unpushed commits plus late receipt appear in persisted debugger dossier/task context, frozen manifest/budgets survive replay. Remaining same proof plumbing through clean filing/close, focused dossier/handoff/exhaustion gate, Ruff/diff/report/commit; no blocker.

Task7a: fix round1/5 (11 addressed,1 open — dossier lacks exact intermediate repair commit range and primary-stage added receipts; commits41723057..29d68bf4). task-7a-rereview-1.md reports no other new Critical/Important. Round2 assigned original implementer from29d68bf4, dossier refresh/range proof and focused regression only. Prior warnings remain deferred minor,7c/9 cross-phase gates explicitly later.

Task7a fix round1 implemented29d68bf4, clean worktree. Final affected aq330 passed/6 skipped, post-gate exact-resource5 passed, repair file33 passed; baseline warnings attributed, Ruff/diffclean. No schema amendment. Controller read full twelve-finding reconciliation + exact commands/output; package review-41723057..29d68bf4.diff dispatched to original /root/review_task7a_sol for scoped rereview. Task7a remains incomplete until findings verdict.

Task7a fix round1 slice5 GREEN: single repair file -k 'retained_dispatch_recovers or scheduler_launches_retained' -x =>4 passed/29 deselected,3 baseline warnings. Crash before CAS, after CAS/before wake and after READY recover; production _execute_task acquisition/preparation/session creation/owner attachment/provider launch plus attached replay preserves HEAD/index/dirty+untracked bytes. Final checks/report/commit underway; no new phase until scoped review.

Task7a fix round1 slice4 GREEN: aq test repair+contracts -k 'start_reports_corrupt or primary_attempt_exhaustion or repair_contracts_expose or debug_dispatch_retains' -x =>9 passed,11 baseline warnings. Typed action Literals, deterministic corrupt-start invariant_error, expanded persisted dossier and retained-proof debug HEAD/task context implemented. Remaining fault-injection boundaries, actual scheduler→attached session test, final focused gate/lint/report/commit, scoped rereview.

Task7a fix round1 slice3 GREEN: single repair file -k 'root_green_waits or dispatch_persists_paused' -x =>2 passed/24 deselected,3 baseline warnings. Exact attached ASSIGNED/IN_PROGRESS replay returns already_dispatched without wake/transfer; same-root-subject bind preserves awaiting_completion/success evidence. Next typed contracts, invariant outcomes, dossier completeness, then crash/launch coverage.

Task7a fix round1 slice2 GREEN: single repair file -k 'running_repair_delegate_files or real_task_close or parent_subject_binding or primary_dispatch_reuses_only' -x =>4 passed/22 deselected,3 baseline warnings. Conn-owned parent exact-generation/head binder preserves same-subject readiness/budgets, invalidates movement, feeds debug start; exact attached existing verifier filing preserves verifier role. Next post-launch dispatch and root binder replay.

Task7a fix round1 slice1 GREEN: single repair file -k 'batch_repair_delegate_can_file or retained_handoff_rejects or retained_stop_proof_rejects or running_repair_delegate_files or real_task_close' -x =>6 passed/19 deselected,3 baseline dependency warnings. Exact session-instance/workspace/fence filing+close CAS, writer-kind/role pairing, desired-stop+instance proof and atomic lifecycle-only close outbox implemented. Next slice parent current-subject and existing-verifier filing; no API blocker. Full fix review still pending. Task7c brief now supplies explicit three-slice order and labels old preflight missing mechanisms as historical, avoiding duplicate7a work.

Task7a initial review: twelve Important findings in task-7a-review.md: parent subject/progression; launched dispatch replay; final attached session/fence filing+close CAS; branchless reused-verifier filing; writer-kind/role pairing; instance-token stop proof; atomic close fact; same-subject root bind replay; dossier completeness; typed actions; corrupt start outcomes; crash/actual-launch coverage. Controller confirmed parent-only missing binder and replay reserved/status restriction with focused code reads; remaining findings supplied with exact cited evidence to original implementer for verification/fix. No Critical. Task7c lifecycle consumption and Task9 caller ordering remain explicit later tasks, not satisfied yet.
Task7a minor (deferred): dependency deprecation warnings remain in recorded test output; final review to triage environment filtering without concealing new runtime warnings.

Task7a implemented: runtime0650d4e8, report41723057; clean worktree. Final aq affected-area gate214 passed/3 skipped, repair file22 passed, SQLite and unique scratchPG migration cycles2 passed; changed-file Ruff/diff clean. Existing dependency deprecations documented. Review package review-22a7f911..41723057.diff dispatched to fresh /root/review_task7a_sol for spec and quality gate. No Task7a completion until review resolves.

Task7a near-final checkpoint: repair file16 pass before final refinements; retained+existing-verifier2 pass; close/retained/green-timeout3 pass; persisted-conflict1 pass; SQLite/unique scratchPG cycles each pass. Repair close bypass and retained exact-workspace preparation implemented. Remaining ordinary self-parent current-head seam, terminal BLOCKED contexts, final repair+affected-area aq gate, Ruff/diff/report/commit. No architectural blocker.

Ruling: Add repair-only retained-workspace primary→fresh-debug handoff after uncached stopped-session proof, atomically reserving the same untouched workspace and advancing ownership fence with durable provenance; keep generic clean detach unchanged — actual deadline exhaustion may leave dirty/unmerged/unpushed repair work, and forcing a clean parent push or skipping debug would violate safety/approved escalation — cost if wrong: specialized claim/preparation/attach recovery path and focused concurrency coverage; no generic ownership relaxation.
Task7a capacity retry succeeded; checkpoint report now exists. Real fresh-head filing1 pass/12 deselected proves current pushed head base, generation advance, unchanged budget and dirty refusal; expired-stage assertion added. Repair close/runtime guards in progress. Retained-workspace handoff proposal approved with strict binding above.

Task7a transient tool/model failure: agent returned 'Selected model is at capacity'. Resumed same /root/task7a_repair_sol with existing workspace, instructed checkpoint report first/no redo. Last confirmed dispatch2 pass/9 deselected. Added concrete runtime-close acceptance after inspecting committed Task6 verifier-only branch; exact repair close must never invoke legacy integrate/implicit parent target push or fabricate delivery/completion. Unfinished-work safe debugger handoff may need a surfaced concrete seam decision; no weakening prescribed yet.

Task7a filing cross-seam: design6.2 requires running writer's current clean/pushed parent HEAD at self-child filing. Controller inspected committed task_commands references: worker filing calls file_prepared_child_on without a checkpoint refresh, so this needs explicit coverage in the touched7a filing path. Added current-head/dirty/unpushed acceptance for ordinary self-parent and operation writer→real-parent filing; initial branchless and explicit batch-root semantics preserved. Not a new policy, fulfills existing design; implementer asked to surface any locking conflict.

Task7a dispatch slice GREEN: pytest single repair file -k 'primary_dispatch_reuses or dispatch_persists' -x,2 pass/9 deselected. Durable PAUSED relationship before handoff, guarded READY/replay, exact live attached verifier reuse with no primary route and preserved verifier role. Remaining human-block/outbox+completion guard, filer/resolver/orphan pause, command runtime callback, migrations/docs/final area. No blocker.

Task7a checkpoint: integration_contracts single-file8 pass; exact parent rollover compatibility1 pass/18 deselected; command authorization target1 pass/8 deselected; repair service8 pass before new dispatch RED. Current slice real persisted PAUSED→ownership handoff→READY dispatch/replay, expected missing-constructor TypeError RED. No blocker.

Ruling: Execute Task9 as two sequential independently reviewed Sol phases (9a authenticated evidence/credential isolation, 9b Git candidates/rebuild/promotion), preserving overall Task9 requirements — separates external-provider trust from Git mutation/recovery into smaller implementer/reviewer contexts — cost if wrong: one extra bounded review handoff, no runtime policy change.

Task7a first service/schema slice GREEN: pytest single tests/test_integration_repair.py -x,5 pass. Covers parent-op reuse, exact trigger/subject, immutable start/deadline replay, due scan without writer/CI, conclusive attempt dedupe/infrastructure exclusion, primary→debug attempt/timeout and dossier. Remaining contracts, awaiting_completion root semantics, batch uniqueness, dispatch, filing, dialect cycles/final area. New unpublished revision7a1d5e9f0b2c in progress.

Task7a orientation checkpoint: agent read briefs/preflights/design; aq prime reports task_id required because delegated shell has no aq task scope. Controller instructed no retry/no daemon task mutation, stop generic mapping and begin first RED slice from existing maps. Task7b brief made standalone plus its single resolution preflight to reduce repeated unrelated reading; no requirements dropped.

Ruling: Treat conclusive green as exact-subject awaiting_completion, not terminal passed until boundary delivery/completion; a root awaiting deterministic promotion does not dispatch debug merely on timeout, but authoritative candidate rebuild resumes the same original deadline/attempts and immediately escalates if overdue; parent final verification remains deadline-bounded — reconciles design's green 'finishes stage' wording with no budget reset on main movement and avoids agents redoing already-green CI — cost if wrong: localized timeout/readiness policy adjustment, no lost budget history.

Task 6: fix round2/5 (1 addressed, 0 open; commits19dd9450..22a7f911). Exact completion proof now consistent across guarded completion, preserved checkpoint pointers, acceptance and readiness. Scoped review task-6-rereview-2.md approved with no new Critical/Important.
Task 6: complete (commits b33f35ec..22a7f911, review clean). Runtimefe55b81e, fix62cad36e, report19dd9450, fix22a7f911. Final carried Minor: focused decomposition of now-large ParentCompletion module for final review. Task7 and Task9 seams remain deliberately unimplemented; no enablement.
Task 7a: starting, BASE22a7f911, durable repair/delegate lifecycle only; task-7a-brief.md plus overall task-7-brief.md and dispatch preflight are binding. Subsequent7b conflict proof and7c playbook/routing separately reviewed.

Task 6 round2 implemented22a7f911 from19dd9450: append-only integration_parent_operation_completions exact composite verification proof, checkpoint retained last-completion pointers, guarded-completion atomic insert, exact carry-forward joins. Two-verification regression RED stale_head then GREEN; combined aq20 pass, SQLite/PG cycles each1 pass, baseline-only deprecations, Ruff/diff clean. Scoped /root/review_task6_sol reviewing review-19dd9450..22a7f911.diff; sole open prior finding is exact completion identity.

Task 6: fix round1/5 (7 addressed, 1 open — carry-forward selects arbitrary prior verification instead of exact guarded-completion verification; runtime fe55b81e..62cad36e, report19dd9450). Round2 assigned original /root/task6_parent_sol, FIX_BASE19dd9450, only exact completion-proof binding and two-verification rollover regression, plus required schema cycle checks. No Critical; same provenance defect is sole new Important in fix diff.

Task 6 warning clarification: final 3/2 warnings are established pkg_resources/zope namespace/audioop dependency deprecations, not new schema/runtime warnings. Exact messages appended to report without runtime changes or repeat tests; scoped reviewer notified.

Task 6 fix round1 scoped re-review active /root/review_task6_sol, runtime range fe55b81e..62cad36e; report-only commit19dd9450 adds exact final commands. Report reveals another multi-file bare pytest invocation (3 final tests across two files) contrary to aq rule, and 3/2 warnings in the two final focused runs. Record process deviation; reviewers should classify warning evidence, not claim pristine output. No repeated run solely to retroactively change command history.

Task 6 fix round1 implemented 62cad36e over fe55b81e; 214 affected-area tests plus 4 post-gate tests and SQLite/PG cycles pass, Ruff/diff clean per report. Review package review-fe55b81e..62cad36e.diff prepared. Requested exact four post-gate commands/output in report before scoped re-review; no further runtime work assigned.

Task 6 fix round1 midpoint: implementer reports green disabled-settlement, stale competing-review, unbound-history rejection, completed-parent rollover/carry-forward and atomic-suspension-rollback regressions. FK/migration, surface and command-level lifecycle remaining. No architectural blocker; historical carried receipts excluded from new squash chain.

Task 6 review cross-task checks: controller inspected unchanged verify_workspace_checkpoint at src/integration/hierarchy.py:81; requires exact owned workspace/repository/task branch, strict current branch, empty porcelain status, HEAD equality and present equal remote ref. This resolves the review's clean-pushed-head cannot-verify seam. Trusted forge observation is explicitly Task9's gate; bounded repair/debug is Task7's gate, both already in their briefs and remain unfinished.

Task 6: initial review needs fixes; eight Important findings (summary mistakenly said seven): disabled settlement, parent episode rollover, non-atomic suspension, receipt context binding, stale review snapshots, relational constraints, surface projection, command-level lifecycle regression. Fix round1/5 assigned original /root/task6_parent_sol, FIX_BASE fe55b81e. Minor deferred: new parent_completion module 868 lines; final review to triage focused decomposition.
Ruling: Bind new receipts to their operation/episode and permit prior delivered children only through immutable explicit carry-forward acceptance from a previously verified/completed episode whose aggregate is proven ancestor of the new checkpoint — current-episode-only receipt matching would require illegal duplicate delivery after parent review rejection, while unbound history is insufficient — cost if wrong: extra episode/receipt provenance links and ancestry validation can be simplified later without rewriting receipts.

Ruling: Add separate integration_repair_start and integration_repair_dispatch typed primitives, using the preflight's arguments/outcomes and frozen object-authorized policy — budget activation must not create an unnecessary initial root repair agent, while dispatch needs atomic durable task binding and replayable handoff/wake — cost if wrong: localized contract/playbook fixture adaptation.
Ruling: Reuse only an exact live attached stage-0 verifier and persist stage writer_kind with its repair_task_id; preserve its verifier ownership role and ordinary parent filing semantics, while debug always gets a fresh writer — avoids redundant primary agents without confusing delegate authority — cost if wrong: additional typed stage field and resolver tests, or a later separate writer column migration.

Task 6: implemented fe55b81e, all five milestones; 232 affected-area tests, 5 reviewer evidence tests, two SQLite/PG migration cycles, changed-file Ruff and diff check pass. Report task-6-report.md; review package review-b33f35ec..fe55b81e.diff; /root/review_task6_sol reviewing. Task9 observation remains a declared fail-closed future seam. Existing review-pipeline e2e fixture imports absent pipeline_compiler; outside scope, focused actual producer covered. One bare two-file pytest deviation recorded and corrected with aq area run.

Ruling: Execute Task7 as three sequential independently reviewed Sol phases (7a repair/delegates, 7b resolution receipts, 7c playbook/routing/provenance), with shared binding Task7 brief — the preflight exposed separable large seams, and smaller review diffs reduce context churn while preserving every deliverable — cost if wrong: two additional bounded review handoffs; no runtime policy change.

Task 1: implemented da4ed753, under review /root/review_task1 (Sol). Initial 9 passed/7 skipped; controller running skipped PG coverage against disposable Docker PostgreSQL.
Task 1: fix round 1/5 dispatched to original Terra implementer. Review findings: PG boolean defaults invalid; sealed member UPDATE escape via NEW.batch_id; missing counter/identity/audit guards; missing per-member revision result storage; parent active repair uniqueness wrongly includes episode. Controller PG run 1 passed/1 error confirms first finding. FIX_BASE da4ed753.
Task 1: fix round 1 implemented 88a99056, 28 SQLite/PG checks + 1 boolean-default check passed; scoped re-review pending. New table integration_candidate_member_results is the per-member/revision application evidence consumed by Task9.
Task 1: fix round 1 review 4 addressed/1 open plus new relational-integrity gap. Fix round2 dispatched: freeze prepared provenance fence/recovery ref; monotone member cursor/stage; member-result composite FKs and precise tests. FIX_BASE 88a99056. Preparation fence remains immutable provenance; current ownership must be independently validated when retrying.
Task 1: round2 implemented 687b4dc3; follow-on migration 9c4a7d2e1b6f. 28 dual-backend +1 boolean checks passed. Scoped re-review pending.
Task 1: round2 review confirms runtime schema mechanics but flags follow-on falsely claiming support for original intermediate 3f schema.
Ruling: Consolidate these unpublished local migration iterations into one final 3f migration and test main-head → final head, including downgrade — no branch changes have been pushed or installed on operator DB, so intermediate implementation commits are not deployment contracts — cost if wrong: a development scratch DB on removed 9c must be recreated, never repair an operator DB automatically.
Task 1: fix round3 requested for migration consolidation/history claim. FIX_BASE 687b4dc3.
Task 1: round3 commits 5dd07c42,b834bc98; 30 SQLite/PG tests including upgrade/downgrade/re-upgrade passed. Final scoped re-review pending. Current PG base DSN uses integration_test_final; original integration_test scratch had intermediate removed revision. Final schema revision remains 3f30b34c7e7c.
Task 1: complete (commits 0c96ac2c..b834bc98, review clean). 30 checks dual backend plus boolean guard. Minor deferred: report old 'fourteen tables' text now fifteen; DBAPIError helper could assert specific trigger identities.
Task 2: in progress; base b834bc98; Sol implementation.
Task2 interface decision: IntegrationOutbox(db, accept_event=runtime.accept_integration_event). Acceptance persists server-owned protected playbook_pending_events rows for all matching destinations then returns; async dispatch does not block tick. No matching destinations leaves outbox pending. Narrow migration adds protected false; TTL/overflow/generic discard must preserve these rows, explicit integration controls remain later work.
Task 2: implemented 2a72be8f; review /root/review_task2 (Terra) pending. 15 focused tests +12 storage regressions +1 Boolean guard passed. Baseline stale tests use removed PlaybooksConfig fields/v2_engine_enabled; no unrelated fixes. Migration a7c4d9e2106b adds protected pending flag. Review package review-b834bc98..2a72be8f.diff.
Task 2: fix round1 dispatched original Sol. Review Critical: replay not pinned to accepted scoped activation/artifact, collides same-playbook scopes. Important: restart drains only first100 once; single-event fanout unbounded. Fix requires pinned destination dispatch, durable paged acceptance, recurring bounded backlog drain. FIX_BASE2a72be8f.
Task2 fix interfaces: protected pending activation_id/artifact_sha256, outbox destination_manifest+acceptance_cursor; CAS snapshot and page32 persistence, pinned engine artifact dispatch and destination-scoped identity. Runtime bounded in-flight replay set fills vacancies per cycle, not gather-all bottleneck. Artifact retention must preserve outstanding references.
Task2 fix round1 implemented853b7a1c, 22 focused+3 fresh PG/SQLite cycle/Boolean checks pass. Scoped re-review /root/review_task2 pending. Agent report notes bare pytest was mistakenly used across two migration-test files; no redundant rerun, instructed aq wrapper for future. FIX_BASE2a72be8f.
Task2 fix round1 review: original3 addressed. New Critical manifest/GC TOCTOU, Important recurring reconciler dies on transient DB error. Round2 dispatched to original Sol, FIX_BASE853b7a1c. Relational artifact pins must commit atomically with manifest and prevent racing collection; retry loop survives transient faults, preserves cancellation.
Ruling: Initial destination snapshot traversal may be O(number of matching activations), while per-cycle acceptance writes and replay jobs remain bounded — capturing an all-destination snapshot inherently visits its entries — cost if wrong: very large activation catalogs may require a DB-native snapshot optimization, not a semantic cap.
Task2 round2 implemented61042a05;26 covering+4 targeted+4 fresh migration checks passed, baseline warnings recorded. Scoped re-review pending. Added integration_outbox_artifact_pins; GC preserves transactional pins and retry loop recovers transient list errors.
Task 2: complete (commits b834bc98..61042a05, review clean). All findings addressed after2 fix rounds. Initial snapshot O(n) ruling remains documented.
Task 3: in progress; base61042a05; Terra implementation. Fresh optional PG base integration_test_phase3 port16833 available.
Tasks 4–12: pending.
Task 4: complete (commits6ac2e941..0f4f0270,review clean after2fix rounds). 220covering initial+47/33amended,2SQLite/PGcycles; historical replay proof explicitly Task12 unresolved, not attributed to baseline.
Task 5: in progress, base0f4f0270, Sol implementation with task-5-brief.md prescriptive map and task-5-preflight.md.
Task5 round1 all5fixes implemented, focused336passed5skipped; final claim coverage/report/commit pending. Collection of old test_claim_queries.py fails removed PlaybookRun import; agent reports it as existing, no unrelated fix.
Task5 round1 committed17aa0cf0; final covering403passed5skipped (includes57claimcommand tests), Ruff/diffclean. Scoped re-review /root/review_task5_sol pending, FIX_BASE131e56bd, package88695bytes. No schema changes in fixes.
Task 5: fix round1/5 (4addressed,2open — delayed stop proof leaves old BUSY agent; new pre-attachment transfer leaks never-started workspace/agent; commits131e56bd..17aa0cf0). Round2 dispatched originalSol FIX_BASE17aa0cf0, exact resource-CAS regressions and 3affected testfiles only.
Task5 round2 milestone: both resource leaks reproduced;4focusedregressions nowpass. Durable handoff agentCAS and reserved/unattached cleanupCAS added, no reconcilerchanges; affected3filearea/report/commit next.
Task5 round2 committedb33f35ec;196affectedtests pass,4focusedpass,Ruff/diffclean. Scoped re-review /root/review_task5_sol pending FIX_BASE17aa0cf0,31427bytepackage.
Task 5: fix round2/5 (2addressed,0open; commits17aa0cf0..b33f35ec). Spec passes/task quality passes with Minor robustness follow-up.
Task 5: minor (deferred): execution.py ws_row uninitialized if guarded get_workspace_for_task raises; initialize before lookup with focused error-path regression while Task6 touches launch. No safety gate reopened; final review sees it.
Task 5: complete (commitsde38e2c6..b33f35ec,review clean of Critical/Important after2fix rounds). Initial403area5skip;196amendedpass; SQLite/PG historicalmigrationproofthroughc7. Task6 begins fromb33f35ec.
Ruling: Task6 may use focused parent_completion.py/review_evidence.py behind the prescribed HierarchyIntegration facade, typed policy in models.py — core/producer work otherwise bloats existing large hierarchy/command modules — cost if wrong: extra internal module seams/imports, no public protocol change.
Ruling: Consumed sweep identity persists as project-unique batch.request_id, including terminal empty audit batches without branch/lease/repair — empty replay otherwise has no durable identity — cost if wrong: one additional terminal lifecycle and request index.
Ruling: Coalesced requests retain first-trigger provenance; do not add trigger-history storage — one request must survive periodic/manual coalescing unchanged — cost if wrong: later trigger provenance is available only in existing command/event logs.
Ruling: Nonempty seal reserves no-stage batch operation and artifact pins, construction starts primary clock; emitted operation_id is real operation with separate batch_id — avoids artifact GC/event identity gap before build — cost if wrong: seal transaction gains operation reservation, not extra repair time.
Ruling: GitHub.com App adapter uses existing async/JWT dependencies and daemon-only owner-readable key reference with refreshed installation tokens; one-shot FD askpass for authenticated Git, no worker token reuse — exact app authority must not leak through ambient credentials — cost if wrong: dedicated credential plumbing and GitHub.com-only initial deployment.
Ruling: Shared trust is .github/agent-queue-integration.json, schema aq.integration-trust.v1, check Agent Queue Integration Attestation; canonical digest-bound v1 payload binds internal and numeric forge repo plus distinct CI/attestation app IDs — daemon and hosted CI cannot share operator DB — cost if wrong: versioned manifest/script compatibility and operator-coordinated rotation.
Ruling: Newest trusted attestation record wins; required evidence may span workflows but must be coherent per workflow attempt — prevents older success masking newer failure without imposing one-workflow projects — cost if wrong: explicit run-attempt correlation complexity.
Ruling: Live capability probe needs operator-preprovisioned scratch repo and separate non-bypass identity; absent matching positive/negative proof blocks enablement; implementation uses fakes only — external provisioning/protection writes are not authorized — cost if wrong: operator setup prerequisite before rollout.
Ruling: Root promotion intents gain normalized kind/batch/revision and reserve per-member receipts (intent receipt points at ordinal0); root finalization is atomic for all members — generic child finalizer cannot prove root shipping — cost if wrong: schema/finalizer dispatch seam, no extra anonymous delivery receipt.
Ruling: Supersede obsolete root intents only after definitive unapplied reconciliation; applied/ambiguous results retain original recovery path — replacement revision otherwise wedges unique target intent or loses delivery proof — cost if wrong: stricter reconciliation can require human intervention rather than blind rebuild.
Task 6: in progress, BASEb33f35ec, prescriptive5milestonebrief plus2preflight reports; all prior tasks1–5 remain completed.
Task6 /root/task6_parent_sol feasibility confirmed, no split requested. Milestone1 schema/policy/evidence/episode TDD started; Task9trusted observation seam acknowledged fail-closed until wired.
Task6 milestone1 green: single focused parent checkpoint test after RED KeyError. Typed parent/root policy/fullArtifactRef, nullableprojectJSON, episode/currentverification schema, parent/episodeunique, append-only evidence/links, RESTRICToperationpins+GCrechecks; firstcheckpoint atomically reserves active no-stageoperation. Full migration/area evidence pending latermilestone.
Task6 milestone2 green reported: all-direct-child receipt/contiguous-chain readiness, missingreceiptwait, dispositionrevision, exactstoredcheck evidence+immutablelinks, liveaggregatecheckpoint, genericforcecompletionguard, newchildstale/waiting, exactactiveoperation/verifierrole completion. delivery.applied operation_id=fenceowner pluspromotion_intent_id. Exact coveringcommands/counts due in finalreport.
Task6 milestone3 midpoint: managed parent/leaf verify-only close wiring, parentPAUSED+reservedrelease, leafliveheadadvance, role-aware attachment/prep/launch and parent/delegate resolution, collector→verifier wake, receiptreadyoutbox, push exclusion implemented. Runtime GREEN not yet claimed; iterating disabled compatibility. Direct launch/suspend/ws_row tests, delegatecreation, prime/settlement/ancestor guard and realreviewproducer remain.
Ruling: Actual child promotion push holds role-aware ownership mutation exclusion; read-only/committed replay remains independent of old fence — controller/implementer confirmed Task4 assert_current→push TOCTOU while wiring Task6 collection — cost if wrong: bounded Git push holds the owner DB transaction, but no nested receipt transaction.
Ruling: Parent aggregate verification gates task completion; later reviewer approval gates upward delivery/root eligibility — task.completed creates the review, so demanding it earlier deadlocks — cost if wrong: ordering must be revisited, not an approval bypass for promotion.
Ruling: Parent collection episode ID persists across generations/retries, with unique parent/episode operation and no clean-collection repair stage — prevents duplicate or reset budgets — cost if wrong: additional checkpoint/operation identity migration.
Ruling: Nullable project hierarchical_integration_policy stores typed parent/root inputs and explicit artifact bindings, snapshotted per operation; missing configuration fails closed — core needs policy before Task11 controls exist — cost if wrong: later config migration, no implicit enablement.
Ruling: Integration suspension uses PAUSED plus durable checkpoint/episode, not manual_pause or fake timer; guarded wake honors real manual holds and orphan recovery excludes integration waits — avoids new public task status and premature retries — cost if wrong: narrow lifecycle guards need rework.
Ruling: Per-child normalized disposition revision supersedes no-op/abandonment receipts without invalidating unaffected siblings; code-delivery immutability remains — parent generation alone cannot prove current disposition — cost if wrong: extra normalized revision schema.
Ruling: Persist immutable verification/evidence links and append-only check evidence; Task9 shared trusted forge producer handles parent and root subjects, Task6 fails closed until then — free-text test claims are not evidence — cost if wrong: CI producer work grows and parent declarations need compatible check execution.
Ruling: Advance live checkpoint_sha after collector handoff/verified fixes while retaining old SHA in immutable provenance; delivery events carry parent operation plus separate intent identity — dynamic children and resumed playbooks require current base/domain identity — cost if wrong: additional provenance projection compatibility work.
Ruling: Operation artifact snapshot uses versioned full ArtifactRef wire values plus normalized GC pins retained with audit; local/service creation requires configured real binding — JSON hash alone does not prevent GC — cost if wrong: artifact retention/storage grows until audit retention is designed.
Ruling: Branchless verifier uses operation.verifier_task_id, repair uses current stage.repair_task_id, both server-resolved originless delegates on exact target — independent origins create self-delivery deadlocks — cost if wrong: scheduler/assignment/workspace resolver gains narrow delegate cases.
Ruling: Freeze explicit primary/verifier route fields in boundary policy; validate necessary routes before enabling, with operator-designated higher debug class and no invented rank — registry contains mappings, not intelligence ordering — cost if wrong: more explicit project configuration rather than guessed profiles.
Ruling: Ship playbooks as reviewed V2 fixture bundles and no enabled activation on seed/import; no new prose lowering — existing compiler requires agent proposal and frontmatter enabled:false is not enforcement — cost if wrong: reviewed artifacts must be regenerated when source/contracts change.
Ruling: New events for existing operations select frozen artifact via stable playbook/scope key while requiring current enabled/ready authorization; missing owner route stays pending, accepted-event replay unchanged — per-event pins alone can switch policy mid-operation after reactivation — cost if wrong: disabled routes intentionally hold operations until authorized resume.
Task 5: review failed five Important findings; fix round1/5 dispatched original /root/task5_hierarchy_sol, FIX_BASE131e56bd. Open: non-slot exact-origin fenced prep; failed/stale startup durable release; push pending-origin readiness; missing verifier fail-closed; bulk hierarchy/graph writer atomic guard. Prescriptive covering tests and no Task6 scope supplied.
Task 5: minor (deferred): two-file bare pytest process deviation accurately recorded; no redundant rerun to rewrite history.
Task 5: round1 milestone: all5findings verified; missing-verifier and enabled bulk/graph guard regressions green. Push pending-origin regression reproduced (task reached PAUSED through workspace prep); shared SQL predicate now wired across claim/scheduler/assignment/execution, targeted run ongoing. Startup release will reuse existing exact-fence transfer/confirmer, unknown stop retains ownership/lock.
Controller verified SQLite historical test truly starts nonexistent DB and command.upgrade head; Task5 report gives freshemptyPG throughc7 evidence. Task4 current-head historical proof gap resolved throughc7, oldfailurecauseunknown. Task12brief updated avoid pointlessbaseline archaeology, finalheadreplaystillappropriate.
Task5 doc-only131e56bd corrected report; runtime reviewhead0afce5f1 unchanged. Report now explicit fresh emptySQLite+PG historical upgrades throughc7;PG commandusedexplicitConfigURL with unrelatedAGENT_QUEUE_DB_URLunset,protected3workerenvsunchanged. Reviewer informed. Some plan-owned reports1/2/5 are tracked; final workspacecleanup must include their tracked deletions without touching sibling planreports.
Task5 committed0afce5f1;external69bd46f3 layoutperf/de38e2c6 main merge landed duringwork, preserved;review scoped base nowde38e2c6. /root/review_task5_sol pending.305runtime+87hierarchy5skip+eventfixture1pass,SQLite/PGmigration checks,Ruff/diffclean. Asked agent doc-only corrections actualtask_integration_checkpoints andc7projectcolumns, exactmigrationevidence, acknowledge barepytest2file processdeviation (no redundant rerun).
Task5 closing milestone:runtimearea305passed,prescribedhierarchy87passed5skip,reconciler-starting stale-scan regression1pass,PGscratchc7upgrade verified columns/default/check. Broaderarchive/eventrun471pass then missing canonical payload fixtures for15integrationevents (14earlier OURbranch+newmaterialization). Directed minimal fixture batch update+focused failedtest rerun, not attribute ourbranch gap as unrelated upstream.
Task5 prescribed area87passed5skipped (hierarchy/hierarchyqueries/branchisolated). RealGit absent-or-exact materialization + dirty/unpushed checkpoint tests; direct/pool launchfencing reported green. Controller asked explicit proof/fix for stale managed-reconciler scan during committed-starting startup before finalreport (no reconciler diff yet).
Task5 controller found pooled-claim bypass claim_commands.py:_prepare_and_activate_locked direct default-base reset; instructed shared exact-origin/fenced prep+existing-session attach for enabled pools with focused tests. Readiness filter alone insufficient. Same requested semantics, no rollout restriction assumed.
Task 5: mutationguard subset3pass;ownership attach/exclusion11pass;workspace_branch_wait7pass including transfer-before-prep no reset. Providerstart fake/tmux/subprocess inspected no AQDB write dependency. Conn-owned sessioncreate/update+startup race tests in progress; clean/pushed checkpoint+strict materialization remain bounded service/Git work.
Task 5: milestone routing green9hierarchy,5targetedguards,6contract(singlefilepytest allowed);ordinary create+proposalbatch conn-owned routing done. Mutation generation/exact-origin workspace/managed launch CAS/races remain. Reminded clean+pushed parent proof and real absent-or-exact branch materialization coverage.
Task 5: milestone1 core7hierarchy+6contract tests pass; schema/model/querymode+repo, topdownorigins/checkpoints/ownership, outbox, generation/reparent/claimguard implemented. Ordinary-create/proposal-batch conn-owned routing next, then materialization/mutation/startup seams.
Task 5: /root/task5_hierarchy_sol feasibility clear, RED missingHierarchyIntegration captured; mode/designated-repo schema+atomic filing+pending-origin claim predicate milestone started. Will check provider.start DB dependency before row-held startup exclusion.
Task 3: complete (commits61042a05..2807daa2,review clean after2 fix rounds). Final188 covering plus31/15 amended runs pass; dependency warnings attributed, no runtime warning change. PG concurrency deferred to Task12.
Task 4: in progress, base2807daa2, Sol implementation with prescriptive brief and preflight decisions.
Task 4: round2 committed0f4f0270;33promotion/contracts+2focused pass,Ruff/diff clean,no schema change. Scoped re-review /root/review_task4_sol pending FIX_BASE14a32e04.
Task 4: round1 review4addressed,2newImportant retry regressions (stale first-time target reserves blocking intent; terminal replay requires old current fence). Round2 dispatched originalSol FIX_BASE14a32e04. Prescribed separate existing terminal/prepared vs new creation paths; terminal replay also survives legitimate source cleanup, retaining object-level auth.
Ruling: Enabled worker launch persists starting-session identity+attachment before process start, then holds same ownership exclusion across bounded startup/running persistence; managed reconciler uses same fence — check-then-start alone races transfer — cost if wrong: short DB writer hold during startup; provider DB dependency must be checked before implementation.
Task 4: round1 committed14a32e04;47passed14skipped+2freshSQLitePGcycles,Ruff/diff clean. Scoped re-review pending /root/review_task4_sol FIX_BASEee865aa3.
Task 4: round1 all4 regressions reported red→green; final covering+fresh migration cycles running. Index/schema/b91 adjusted for exact review tuple. No extra scope.
Task 4: review4Important (public ambiguous-push command replay; exact-tuple lookup ordering; nonterminating paths-heavy diagnostic bound; missing different-domain concurrency/advanced clean target tests). Round1/5 dispatched /root/task4_promotion_sol FIX_BASEee865aa3. b91 coherent revision-locally; historical replay proof gap assigned Task12, attribution unverified.
Task 4: implementedee865aa3 onbase6ac2e941;220passed14skipped covering+2SQLite/PG migration cycles, Ruff/diff clean. Review /root/review_task4_sol pending. Report alleges pre-existing historical dep_type ordering issue; asked for exact prior evidence or mark attribution unverified, no extra full replay.
Task 4: final covering220passed14skipped, freshSQLite+PG cycles2passed including PG evidence append-only. Late mark_pushed/mark_prepared cannot reopen committed intent. Final lint/commit/report pending.
Task 4:20 promotion tests+199 covering14skipped reported pass;SQLite+PG cycle and PG append-only checks pass. Self-review caught late mark_pushed after finalization; one monotonic state regression being fixed before final commit.
Ruling: Parent collection reserves the existing target-bound operation before any repair stage exists; first actual failure activates its primary budget — Task3 requires persisted collector identity but clean sibling delivery must not start repair clocks — cost if wrong: operation status projection must distinguish collecting/no-stage from active repair.
Task 4: second milestone6 crash phases+negative Git/source/target/origin/evidence cases reported green, typed handler/SESSION denial green, SQLite+PG b91 upgrade/downgrade/upgrade pass. Final PG evidence assertion+covering run/report pending.
Task 4: real clean promotion milestone green (2 query+1 real Git endtoend+2 Git primitive tests); negative/crash/authority paths next.
Ruling: Review evidence uses source checkpoint generation, not parent's origin creation_generation; evidence and terminal verdict transitions atomic; reject-then-close cannot mint approval — existing reviewer protocol closes pass on both verdicts — cost if wrong: narrower completion hook requires refactoring transaction seam, without new review protocol.
Ruling: Administrative closes do not mint exact-SHA review approval; enabled final-review graph must identify one task/aggregate — no reviewer session provenance exists for operator close — cost if wrong: operators must request a real review instead of bypassing evidence.
Task 4: /root/task4_promotion_sol first milestone scoped async Git input/env/status+remote-ref helpers2 tests pass; schema/query layer in progress. /root/review_evidence_preflight_sol read-only mapping actual reviewer completion producer for Task6.
Task 4: shared HEAD externally advanced via fd80d633 merge main and6ac2e941 migration-head merge while implementation was beginning. Preserve those commits; scoped Task4 base now6ac2e941, Alembic parentf02a4a4a3010. Agent asked to confirm origin; no controller merge/rebase performed. Previously untracked dashboard docs now tracked through external main merge.
Task 3: round2 implemented2807daa2,15 transfer tests pass,lint/diff clean; scoped re-review pending.
Task 3: round1 review3 addressed, new Important BLOCKED repair task accepted by terminal validation. Round2 dispatched original Sol, FIX_BASE22e17ba1; narrow handler+parameterized tests only.
Task 3: round1 implemented22e17ba1,31 amended tests pass; Ruff/diff clean; warnings attributed to pkg_resources/zope/audioop dependency boundaries. Scoped re-review /root/review_task3_sol pending, FIX_BASE14050ba8.
Task 3: review found Critical released-state old-token authority; Important physical-detach-before-release crash recovery and missing persisted repair-stage relationship. Fix round1/5 dispatched /root/task3_sol, FIX_BASE14050ba8. Minor deferred warning attribution; ask agent to name warnings. Cross-task attachment wiring carried Task5; promotion helper use Task4/9; PG concurrency validation Task12.
Task 3: implemented14050ba8 by Sol continuation;188 focused tests reported pass, changed-file Ruff/diff clean. Independent review pending. Ownership release evidence transaction is in workspace_attachments.py; expected-target helper verifies BOTH base and expected-old ancestry.
Ruling: Task4 gets a daemon-owned full-object bare cache keyed by hash of canonical repo ID, not a slot or inferred workspace — no existing durable repo-to-workspace mapping — cost if wrong: extra disk/fetch overhead, configurable storage relocation later.
Ruling: Add append-only reviewed-source evidence and frozen intent authors/provenance in Task4; Task6 writes it at trusted review completion — existing generic completion is not exact-SHA approval — cost if wrong: an additional schema/API seam, with promotion safely blocked until producer lands.
Ruling: Freeze deterministic squash identity, integer timestamp, receipt-bearing message and 64KiB diagnostic bound as specified in Task4 brief — prepared OIDs must survive retry — cost if wrong: commit attribution/display convention can change only for future intents.
Ruling: Current playbook invocation provenance is nullable until Task7 binds server-only StepContext metadata; never substitute parent_run_id — existing adapter lacks current run identity — cost if wrong: intermediate feature-disabled intents lack optional run metadata, task/session evidence still retained.
Task 3: continuation /root/task3_sol; bounded read-only Task4 preflight /root/promotion_preflight_sol.
Ruling: PLAYBOOK transfer authority is explicit capability plus exact project scope and server-validated target-bound task/operation; parent_run_id is provenance, not an integration operation ID — existing principal model has no such equivalence — cost if wrong: a future finer-grained grant can narrow project-scoped orchestration without changing domain ownership.
Task 3: Terra stopped safely at user request with uncommitted ownership/Git/callback/contract drafts; 4 ownership and 2 Git focused tests passed. Sol takes over using prescriptive continuation in task-3-brief.md; base remains61042a05.
Ruling: Use Sol for implementation and review going forward, per user's explicit preference; assignments specify files, interfaces, prohibited scope and acceptance tests — cost if wrong: revisit model choice only if a concrete blocker requires it.

Preflight evidence: existing V2PlaybookRuntime._on_event schedules background dispatch and _dispatch swallows exceptions. Task 2 cannot acknowledge from EventBus.emit; must use durable acceptance.
Preflight evidence: proposal_commands._cmd_task_batch_commit uses compensation across task creation, not one transaction. Task 5 must provide actual atomic integration child filing rather than just incrementing generation around the legacy loop.
Brief helper cannot parse numbered section headings ('## 5. Task 1'); controller extracted exact task blocks and shared constraints into the plan-owned brief files using apply_patch.

Disposable test PostgreSQL container: 65b3e4ca145740695d4910d0c653d63c190b6037e7a54407e76d382cb201b761, port 16833, --rm. Only integration_test scratch database, dummy credentials integration_test/integration_test. Stop this exact container at final cleanup; no production data/volume attached.
PG test process session: 28227.
Task 8a: complete (commits 77dc4ea7..44018637, independent review clean after one fix round). Durable per-project scheduling, request coalescing and the integration batch schema are implemented; legacy nonempty NULL Git identity fails before DDL rather than inventing provenance.
Ruling: A nonempty sealed batch retains its outstanding integration request until Task10 releases the finished train; an empty terminal sweep clears it immediately — clearing at seal would permit an overlapping sweep while construction/CI/promotion is still in flight — cost if wrong: the retained request becomes a narrow release/recovery responsibility in Task10.
Ruling: All review-evidence append writers take the project hierarchy mutation lock before inserting, and generated integration refs use hashed project/request identities — sealing eligibility must not race a late review and raw project identifiers are unsafe Git ref components — cost if wrong: minor lock contention and less human-readable refs.
Task 8b: complete (commits 96771800..5d8f84cc, independent rereview PASS with 0 Critical/Important). Atomic exact-frontier sealing covers empty, single, paged >200, busy/replay/rollback, concurrent hierarchy/review changes and SQLite/PostgreSQL ordering; final gates: 23 passed/6 skipped, PG races 2 passed/2 skipped, shared compatibility 74 passed, event schemas 416 passed, Ruff/diff clean.
Task 9a: in progress from BASE 5d8f84cc; Sol implementation followed by separate Sol review. Scope is authenticated GitHub App/check evidence, one-shot credential-isolated Git transport, trust/attestation contracts and shared parent/root CI observation only.
Task 9a: initial implementation commits 205f825d,dc751f7f and report 5afb085b; 321 affected-area plus 168 post-packaging tests pass, Ruff/diff clean. One askpass test hang was root-caused to a test pipe writer held open; production-shaped TemporaryFile regression and child kill/reap cleanup pass promptly. Independent review ded1788e FAIL: 1 Critical, 3 Important, 1 Minor. Fix round1 assigned to original Sol implementer.
Ruling: Privileged GitHub App pushes must execute from a daemon-owned configuration-isolated Git context with an allowlisted environment, no checkout hooks/config/URL rewrites/helpers/proxies, one-consumer credential transport, and whole-process-group cleanup — a frozen URL alone does not constrain Git when the worker controls the checkout — cost if wrong: one extra temporary/bare object-transfer step and stricter Git invocation plumbing.
Ruling: CI observation must acquire project hierarchy authority first and prove the current live parent episode or batch revision plus designated canonical repository before append — typed caller subjects do not establish currentness or repository identity — cost if wrong: additional row locks/lookups on each CI observation.
Ruling: An exact-name/exact-producer record with an invalid ordering ID makes newest selection unknowable and therefore fails closed for attestation read, CI checks, and publication reuse — filtering malformed candidates before ordering can revive an older success — cost if wrong: provider schema anomalies block integration until corrected.
Ruling: integration.github_app is a closed non-secret mapping at load/update/schema/read boundaries; unknown or inline secret-like fields are rejected and never echoed — ignoring unknown YAML keys leaves forbidden secret material durable and UI-readable — cost if wrong: configurations with tolerated typos/extension keys must be corrected before startup.
Task 9a: complete (initial 205f825d,dc751f7f; fixes 32c27e7d,065cbe45,808c7089; final rereview e7741233 PASS with 0 Critical/Important). Evidence: initial 321+168 passes; fix gates 384,182,186 passes; changed-file Ruff/diff clean. The final request-only FD broker binds release to the pinned trusted Git HTTPS topology, sends token bytes once over a helper-private reply FD, zeroizes all paths, and leaves no worker-controlled hook/config/env transport seam.
Ruling: Credential-broker review threat scope ends at the isolated pinned system Git process boundary once every worker-controlled descendant injection seam is removed; assuming arbitrary native code already executes inside that trusted group is equivalent to daemon/trusted-binary compromise and is not a reachable checkout input — otherwise every askpass/RPC design is impersonable and OS sandboxing becomes a different architecture — cost if wrong: add a dedicated sandboxed transport boundary rather than more forgeable lineage checks.
Ruling: Task11 enablement must verify the installed askpass helper and containing directories are outside worker write authority and that the supported root-owned Git HTTPS helper topology matches Task9a pins; unsafe/unsupported layouts fail closed — runtime inode/content checks cannot establish deployment authority alone — cost if wrong: some development/user installs remain observe-only until packaged safely.
Task 9b: in progress from BASE e7741233 after preserving externally merged main/migration/dashboard commits. Scope is ordered candidate construction, bounded roll-forward repair/rebuild, normalized root intents/all-member receipts, and exact tested-SHA fast-forward promotion; separate Sol review required.
Ruling: Execute Task9b as two sequential independently reviewed Sol phases: 9b1 candidate build/rebuild/repair and 9b2 root intent/reconciliation/main promotion — both halves are large state machines with different Git and database failure boundaries — cost if wrong: one extra review handoff, no policy change.
Task 9b1: initial implementation 443e518d/report b1390aec; 13 focused and 267 affected-area tests pass, no migration. Independent review 399c400e FAIL: 2 Critical/3 Important/1 Minor. Fix round1 assigned to original Sol implementer; open areas are current lease/revision/branch ownership, authoritative repair reservation/object handoff/reserved paths, mandatory authenticated repository-bound reads/publication, durable PR/ref reconciliation, and exact sealed delta/rebuild-base derivation.
Ruling: Candidate external mutations use hierarchy-first snapshots plus exact project-lease and integration-branch fences, and reacquire mutation exclusion/current revision immediately around each bounded ref/PR boundary; a repair writer must transfer back before construction resumes — one early snapshot cannot authorize a long build — cost if wrong: more CAS/reconciliation phases and bounded row-lock holds.
Ruling: Root candidate repair results require a server-owned durable reservation bound to current operation/stage/task/session/workspace/fence and an explicit daemon object import, followed by reserved-path and exact-delta validation — caller-supplied lineage and path equality are not authority — cost if wrong: a focused reservation schema/command seam and object-transfer step.
Ruling: Every nonempty candidate uses mandatory exact-repository GitHub App reads/writes and a repository-bound forge provider; candidate ref plus full PR identity/idempotency state persist and reconcile across restart — optional publication cannot represent a built integration candidate — cost if wrong: private/local-only integrations need an explicit future provider rather than silently skipping publication.
Ruling: Candidate application derives the exact sealed source_base_sha..reviewed_head_sha delta, and rebuild base is authenticated from the designated default branch before superseding the current revision — inferred merge bases and caller OIDs can include unreviewed history or strand the batch — cost if wrong: extra authenticated ref read and exact patch/tree validation.
Task 9b1: fix round1 runtime33bd7534/report00464b71; 97 affected-area tests plus SQLite/PostgreSQL migration cycles reported pass. Independent rereview2a26c5f8 FAIL: 3 Critical/3 Important. Remaining: exact operation-stage/concurrent construction authority; full reservation identity and crash-safe partial/repair publication; normalized accepted-repair replay; immutable/reconciled PR identity; caller cancellation preservation; and removal of network awaits from DB transactions.
Ruling: Ephemeral candidate/ref writes use a durable time-bounded mutation claim: reserve under hierarchy-first exact authority, commit, do authenticated network I/O, then hierarchy-first reconcile/CAS; every authority-changing path honors the live claim and expired claims reconcile the remote before takeover — holding database locks over forge/Git latency stalls all writers, while a snapshot alone permits stale writes — cost if wrong: extra mutation state and replay branches can later be collapsed behind one reusable abstraction.
Ruling: Rebuilt conflict and repair publication targets are revision/member/reservation-qualified and replace only an authenticated persisted expected-old integration tip; normalized accepted resolution rows are the sole repair-lineage source for N+1 — reusing one mutable repair ref or removed JSON lineage loses exact history — cost if wrong: more retained audit refs and object imports until terminal cleanup.
Task 9b2 preflight: existing isolated exact-OID push, authenticated ref read, candidate evidence, sealed member/result and child-intent seams are reusable; missing work is trusted-green projection, typed root intent/member receipt reservations, durable claimed main write, root-only atomic finalizer, reconciliation/supersession and strict command registration. Begin only after reviewed Task9b1 claim/authority APIs stabilize.
Ruling: Root promotion gets a typed immutable root intent plus one normalized receipt reservation per sealed member; existing child finalization hard-rejects root intents and a root-only transaction writes the full receipt/outbox set — adapting the one-receipt child finalizer risks partial root delivery — cost if wrong: one parallel finalizer and schema discriminator can later share lower-level helpers.
Ruling: Main-write reconciliation distinguishes candidate applied/reachable, provably unattempted+unapplied, active claim, and ambiguous/unavailable history; only the second may supersede, while ambiguity remains `reconciliation_blocked` — treating moved main as simple failure can either duplicate delivery or strand an already-applied train — cost if wrong: an extra durable pre-write phase and operator-visible blocked outcome.
Task 9b1 fix round2 implemented c5f0feda/report3126606a: normalized durable candidate/ref mutation claims; no network under DB locks; exact construction/conflict/repair authority and replay; accepted normalized repair carry-forward; immutable/fresh-provider PR replay; caller cancellation preservation. Focused finding regressions green, final affected gate96 pass, SQLite/PG migration cycles2 pass, Ruff/compile/head/diff clean. Scoped independent rereview pending for range2a26c5f8..c5f0feda. Implementer reports pre-existing SQLite-to-PostgreSQL copy adapter omits all hierarchical integration tables; defer attribution/scope decision to reviewer/final Task12 unless this diff regresses it.
Task 9b1 rereview2 commit96bf6a1c FAIL: 2 Critical/2 Important. Approved now: accepted normalized repair N+1 replay, durable PR identity/fresh-provider replay, caller cancellation, and no network under DB locks. Open: rebuild/invalidation live-claim query not atomic; 60s claim/lease can expire inside 120s Git; repair ownership transfer precedes durable handoff and cannot replay; final publication ignores accepted-head current tip; immutable workspace path absent; migration fabricates qualified target for legacy pushed rows and lacks explicit irreconstructible-row diagnostic. Fix round3 assigned original Sol implementer.
Ruling: External mutation start requires an atomically renewed project lease and claim window covering the actual transport timeout plus margin; after expiry a worker may only perform authenticated observation and durable reconciliation, never initiate a push — a claim shorter than Git timeout creates an authorized remote write that local state must reject — cost if wrong: modest lease extension and a separate observation-only recovery path.
Ruling: Repair-to-collector ownership transfer and durable handoff reservation/persisted collector fence occur in one hierarchy-first local transaction before network publication; replay uses that canonical handoff instead of the obsolete repair fence — transfer-first leaves a permanent crash window — cost if wrong: a conn-owned ownership helper and extra handoff state.
Task 10 read-only preflight: reuse reviewed Task9 root finalization/unresolved-mutation APIs; add active-vs-catch-up schedule state and bounded queries; one CAS release transaction; bounded nonoverlapping IntegrationService ticks; durable frozen-artifact outbox routing; disabled reviewed prose artifact; exact candidate-tree trust/attestation workflow with no post-main CI; normalized independent cleanup; frozen move/cleanup policy and worker credential-isolation tests.
Ruling: Cleanup is normalized retry work independent of the terminal promoted batch and released project lease; exact-SHA deletes use isolated App auth and moved refs become terminal visible conflicts — keeping cleanup in the active lifecycle would serialize new trains on best-effort deletions — cost if wrong: an extra cleanup table and retry scan.
Ruling: A trigger arriving during an active train records only the first durable catch-up provenance while preserving the current request; atomic release converts it into exactly one new sweep — clearing or replacing the active request early permits overlap, while dropping all ticks misses periodic work — cost if wrong: one nullable catch-up tuple and release branch.
Task 9b1 fix round3 implemented d695789e/reporta8bb75b4: same-transaction rebuild claim blocker; 135s claim and prepush lease sufficiency; expired-lease observation-only recovery; atomic repair handoff transfer+collector fence and fresh-service crash matrix; current-revision repaired expected-old; immutable normalized workspace path; legacy pushed resolution provenance and deliberate migration diagnostic. Final affected gate105 pass; seeded SQLite/PG cycles2 pass; Ruff/compile/head/diff clean. Scoped rereview pending range96bf6a1c..d695789e.
Task 9b1 rereview3 commit0d9f7b98 FAIL: 2 Critical/2 Important. Approved: rebuild atomic blocker, timing/expired observation, workspace provenance, final expected-old, and upgrade compatibility. Open: stage/ordinary+confirmed transfer ignore expired ambiguous reserved claims; collector transfer commits before repair_handoff mutation reservation; production mutating confirmer returns stale pre-callback row; downgrade drops non-null handoff provenance. Fix round4 assigned original Sol implementer.
Ruling: `state=reserved`, not claim wall-clock expiry, defines an unresolved external mutation for every invalidator; expiry permits a reconciler takeover but never stage/revision/ownership change until exact remote observation resolves the row — an expired ambiguous push is still ambiguous — cost if wrong: operations can remain visibly blocked for human reconciliation instead of advancing unsafely.
Task 11 read-only preflight: rollout uses existing hierarchical mode plus desired/draining/audit/waiver/probe records; credential config closed+restart-required; shared installed transport and worker-write-authority inspection; GitHub.com typed protection digest; explicit replayable positive+negative scratch probe; DB-only status/observe; full policy/route/trust validation; atomic cutover/legacy merge bypass/drain rollback; generic operator commands/CLI; read-only doctor and operator guide.
Ruling: A same-UID or bypass-capable worker deployment cannot pass train enablement merely because askpass is mode0700 or digest-pinned; the helper, ancestors, Git HTTPS topology and key paths must be outside every effective worker write authority, with the accepted transport fingerprint frozen at cutover and checked before credentialed mutation — runtime pins prevent substitution after launch but cannot prove deployment separation — cost if wrong: development/editable installs remain observe-only until packaged confinement or separate daemon/worker authority is available.
Ruling: Train enablement requires an explicit durable scratch probe with both the configured integration App succeeding and a distinct repository-capable non-bypass identity being rejected under equivalent protection; enable never silently runs the probe — positive success alone cannot prove branch protection rejects ordinary writers — cost if wrong: extra operator setup and retained scratch audit commit.
Task 9b1 fix round4 implemented a55c8085/report95e2ac92: every reserved claim blocks invalidators regardless expiry; repair_handoff reservation+collector transfer+persisted fence atomic; canonical production mutating confirmer first-try/replay; downgrade refuses live handoff provenance. Writer-first and invalidator-first four-path matrices, fresh-service handoff interleaving, 97 area tests and dual SQLite/PG migration tests pass; Ruff/compile/head/diff clean. Scoped rereview pending range0d9f7b98..a55c8085.
Task 9b1 rereview4 commit53964e55 FAIL: 0 Critical/1 Important. All targeted round4 findings approved. Sole new issue: concurrent repair-acceptance rereaders adopt the same live mutation nonce, so two calls can both push or leak stale/Git errors. Fix round5 assigned original Sol implementer for exclusive insert-or-expired-CAS executor ownership, observation-only nonowners and canonical losing-CAS replay.
Ruling: A durable mutation identity is shared for idempotency, but its execution nonce is an exclusive time-bounded capability acquired only by row insertion or affected-row-checked expired takeover; canonical readers may observe/reconcile but never adopt a live nonce — otherwise idempotent replay becomes concurrent write authority — cost if wrong: duplicate calls wait until the bounded claim expires instead of opportunistically sharing execution.
Task 12 read-only preflight: start only after9b1/9b2/10/11; first add the full final Task7–11 table graph to canonical SQLite→PG copy and eliminate/deprecate stale script duplication; compose real hierarchy+train E2E; two-root conflict/primary→debug roll-forward/exact-one-main/no-post-CI; fresh-instance boundary matrix; deterministic PG two-ordering locks; cleanup/release/catch-up/disable compatibility; final-head replay and exact isolated Tier1 daemon smoke. Full suite/stress/perf CI-only; live forge/probe/cutover operator-only.
Task 9b1 fix round5 implemented795db5b6/report025565a4: mutation executor nonce exclusive to inserter or exact expired CAS takeover; live readers observe/wait; desired/lost-response reconcile without repush; unexpected remains blocked; losing acceptance CAS canonicalized. Candidate file54 pass, final area103 pass, Ruff/compile/head/diff clean; no schema change. Final scoped rereview pending range53964e55..795db5b6.
Task 9b1 rereview5 commit9fa29cf9 FAIL: 0 Critical/1 Important. Exclusive executor protocol approved. Sole integration gap: public build observes any reserved row then returns wait, so an expired exact-expected normal candidate partial/final claim never reaches the reviewed CAS takeover (private helper tests masked it). Fix round6 assigned original Sol implementer with public-path partial/final two-service crash recovery and negative blockers.
Task 9b1 fix round6 logical base9fa29cf9; unrelated origin/main merge80034ced landed before runtime commit and was preserved. Runtimec5ca1ea3/report8c3eb23b: structured public observer routes only exact current-phase expired-expected claims into reviewed CAS takeover; live/unexpected/failed/insufficient/out-of-order block; desired reconciles push-free. Focused21, candidate62, final area111 pass; Ruff/compile/head/diff clean. Final rereview should scope80034ced..c5ca1ea3 while checking logical prior finding.
Task 9b1 COMPLETE at0694526f: final rereview6 PASS 0 Critical/0 Important/0 Minor for runtimec5ca1ea3. Ordered candidate build, authenticated ref/PR publication, exact conflict repair/handoff, accepted repair rebuild, durable exclusive mutation claims, crash reconciliation and bounded authority are approved. Task9b2 may begin from0694526f.
Task 9b2 implemented runtimec84d367d/report69c645d6 from0694526f: typed root intents and normalized member receipt reservations; exact aggregate-green projection; server-derived root prepare; shared root_main durable claim/prewrite; exact tested-SHA expected-old push; applied/descendant/unattempted/ambiguous reconciliation; atomic all-member receipts/outbox/finalization; strict command and child-finalizer rejection. Final area222 pass, root file16+concurrency1 pass, SQLite/PG migration2 pass, Ruff/compile/sole head/diff clean. Independent review pending; large 1112-line single-purpose RootPromotionService flagged for audit/decomposition only if needed.
Task 9b2 initial reviewd20f2c13 FAIL: 3 Critical/3 Important/1 Minor. Critical: generic CandidateService can consume root_main and strand intent; prewrite-marked expired claim can repush ambiguously; current moved-main claim/rebuild circularly block. Important: descendant not App-auth imported before real ancestry; finalizer/schema do not prove complete exact reservation/member set; SQLite receipt append-only triggers lost by batch alter. Task10 preflight additionally requires mandatory pre-main attestation boundary, terminal promoted+pending-cleanup handoff and registered operation-bound events. Split corrections into independently reviewed fix1a protocol/attestation and fix1b schema/finalizer/events.
Ruling: `root_main` claims have exclusive reconciliation ownership in RootPromotionService; CandidateService treats them as blockers and never consumes their remote fact — cross-purpose generic observation can advance half of a protocol without its intent/finalizer — cost if wrong: one explicit purpose filter/dispatcher instead of a generic mutation observer.
Ruling: `prewrite_at` is an irreversible ambiguity boundary: only unmarked expired claims can rotate executor or supersede; marked claims require authenticated candidate/descendant proof or remain blocked — an expired timer cannot prove an in-flight/lost-response main write did not occur — cost if wrong: human reconciliation for a rare crash immediately after marker and before a provably absent write.
Task10 decomposed after current-interface preflight into sequential independent reviews:10a bounded schedule/catch-up/selectors/IntegrationService/outbox substrate with root-main dormant;10b candidate-tree trust, durable exact attestation, pre-main gate, workflow and credential isolation;10c typed root routes, atomic release/catch-up, normalized exact cleanup, event schemas and disabled reviewed playbook. All wait for approved Task9b2 terminal/event/attestation seams.
Task 9b2 fix1a implemented464730aa/reportb00cdf05 fromd20f2c13: CandidateService purpose-isolates root_main; root service resumes applied claim; prewrite-null-only takeover with horizon and ambiguity block; current moved-main expired/unmarked supersedes then public rebuild N+1; App-auth exact descendant import/pin+real ancestry; mandatory immutable exact attestation proof gate for prepare/prewrite/command. Final area238 pass, focused public crash/liveness/attestation/realGit green, Ruff/compile/head/diff clean; no schema. Independent fix1a review pending; fix1b untouched.
Task 9b2 fix1a reviewf421e2e8 FAIL: 1 Critical/0 Important. Purpose isolation, moved-main public rebuild, descendant import, attestation, and prewrite no-takeover approved. Sole horizon defect: prewrite checks125s before local ancestry plus possible3x30s token refresh, then starts a120s push after authority may expire. Round2 moves ancestry/token before final prewrite revalidation so only bounded push follows marker, with clock-advancing cache-miss regression.
Task 9b2 fix1a round2 implemented3573ee90/reportf5d33b44 fromf421e2e8: ancestry+token refresh precede final prewrite CAS; horizon/deadline sampled inside hierarchy lock; insufficient claim/lease yields prewrite NULL+zero push; exact post-refresh125s permits one push. Focused8 and final area242 pass; Ruff/compile/head/diff clean; no schema. Independent narrow rereview pending; fix1b untouched.
Task 9b2 fix1a round2 review177a02b0 FAIL: 1 Critical/0 Important. Caller ordering/timestamp approved, but Task9a push resets120s for init/local fetch/rev-parse/exec-path then a fresh120s remote push, so125s horizon does not bound post-marker sequence. Round3 uses one aggregate transport deadline plus cleanup margin exported from GitManager and production-boundary delayed-preparation tests; no blind timeout summation.
Task 9b2 fix1a round3 implemented2520efbc/reportcd8fe1fc; 270 affected-area tests passed. Independent reviewcfcd0fc5: transport aggregate deadline approved, one Critical remains at prewrite DB commit/dispatch handoff, which currently restarts the maximum. Round4 assigned to fresh Sol implementer with absolute deadline across that boundary and production root-to-Git delay tests.
Ruling: Permit an internal tightening-only absolute deadline on the daemon Git interface, capped by the existing aggregate maximum, while retaining no timeout authority in external command contracts — this consumes commit/dispatch delay without enlarging write authority — cost if wrong: a small internal transport signature extension requires adjustment; existing callers keep bounded defaults.
Task 9b2 fix1a: complete (round4 runtime661fde08/reportf3facfb9, independent reviewbb16874e PASS Critical0/Important0; 277 affected-area tests). Original prewrite deadline now survives commit/dispatch and caps the complete transport plus reserved cleanup. Fix1b started from bb16874e with original Sol implementer.
Task10 briefs prepared in b83b1f43, corrected in38c58e18/ffef13b3 before dispatch: approved default is rebuild, one service tick driver, atomic expected-old remote deletion plus local ref coverage. Three phases remain sequential after fix1b review.
Ruling: Task11 uses a separate closed scratch_probe config, explicit blocker-digest-bound history-waiver command, and project-scoped legacy routing suppression; effective/desired modes stay separate and non-train flushes are read-only — these supply concrete interfaces for the existing approved rollout requirements — cost if wrong: adjust the new CLI/config shape before operator adoption.
Task 9b2: complete (fix1b runtimea0115bd1/report34f21559; reviewdf2bfc0d PASS Critical0/Important0/Minor1). Root finalizer/schema/events approved, 251 area tests plus SQLite/PG migrations green. Root promotion complete through exact attestation gate and atomic all-member delivery.
Task 9b2: minor (deferred to Task12/final review): expand PostgreSQL legacy incompatibility cases beyond cross-intent and assert receipt UPDATE/DELETE refusal at intermediate downgraded d4 on both dialects; see task-9b2-fix-1b-review.md.
Task 10a: in progress fromdf2bfc0d with Sol task7c_playbook_sol; bounded service/schedule/catch-up/outbox substrate only. Tasks10b/10c/11/12 remain.
Final-review baseline evidence: local main is stale69bd46f3; existing origin/main904181fd is an ancestor of this branch and includes preserved unrelated main integrations. Use the current verified origin/main merge-base for the feature review rather than stale local main; no fetch/ref update was performed for this check.
Task10a runtimeed12434d/reportc3892b41:136 area passes, dual migration2 passes. Review6c4cc883 FAIL C0/I3/M1: promoted-before-release catch-up omitted, service cursors reset each tick starve persistent later rows, selector error permanently kills loop. Fix round1 assigned original Sol with actual scheduler/multi-tick/background-loop regression tests. Warning provenance Minor addressed by report clarification, not suppression. Parent receipt-fixture failures and fresh-empty migration-chain proof explicitly carried in Task12 brief05181cf4.
Task10a fix round1/5 runtime33460757/report82bd825a,141 area passes; reviewccaedd0d C0/I1: fair cursors and resilient loop approved, catch-up still incorrectly depends on cleanup pending. Fix2 removes cleanup-state dependency while exact old request remains outstanding; focused scheduler ordering matrix only.
Task10a: complete (runtimeed12434d, fixes33460757/e2d01b70; review71ee5db7 PASS C0/I0/M0;141 area passes then15 focused scheduler passes). Bounded fair resilient service, first catch-up until release, frozen policies and dual-dialect migration approved.
Task10b: in progress from71ee5db7 with Sol task7b_resolution_sol; candidate-tree attestation provider/workflow/isolation. Task10c release/cleanup/routes, Task11 controls and Task12 final validation remain.
Task10b runtime26c9e463/report1cfb6ca8,198 area passes; review15bbe83b FAIL C1/I4/M1: fork-prefix suppresses CI, shared-suite verifier rejects valid payload, concurrent attestation POST lacks durable exclusion/main gate, root command has no configured App client, loose App-ID filtering splices proof metadata. Fix1 assigned original Sol fromcd045759 with public-path tests and publication reservation schema.
Ruling: Add a durable exclusive attestation publication reservation with an irreversible pre-POST marker and main/invalidation exclusion — provider external_id is not uniqueness and list-then-create races — cost if wrong: one extra normalized state table/migration and blocked reconciliation for ambiguous provider writes.
Task10b limitation: ordinary task PRs retain full-CI fallback because hosted CI lacks authenticated task-declared focused-check transport; do not claim focused task-PR routing in completion documentation. Missing real trust/App/workflow variables stay fail-closed until operator cutover.
Task10b fix1 implementedabf25a45/report4e604bbb:316 area passes, SQLite/PG publication migration1 each, static checks green. Re-review pending ofc6dabb13..4e604bbb; precedingc6dabb13 is unrelated user roadmap docs/superpowers/specs/2026-09-06-release-roadmap.md, preserved and excluded from phase attribution. New headf0a1b2c3d4e5 adds durable publication reservations consumed by later release/controls.
Task10b fix1 reviewe9af1da5 C0/I1: fork/shared-suite/strict-ID/client wiring approved; publication final CAS omitted exact execution nonce, allowing live former owner after expired unmarked takeover. Fix2 assigned original Sol for terminal nonce fence and public paused-old-owner regression; focused attestation tests only.
Task10b: complete (runtime26c9e463, fixesabf25a45/3300488e; reviewa01048e5 PASS C0/I0;316 area passes then18 focused attestation passes; dual migration). Exact trust/publication/workflow/client wiring approved. Nonblocking dependency warning noise remains documented; ordinary task-PR full fallback is an explicit limitation.
Task10c: in progress froma01048e5 with Sol task7c_playbook_sol; root routes/disabled playbook/release/catch-up/normalized cleanup. Tasks11/12 and final branch review remain after10c.
Ruling: Add subject-only integration_cleanup(batch_id) as the trusted V2 cleanup_requested target, authorized independently of completed operation/released lease. The normalized selector cannot discover rows before materialization, so this fills the required event-to-service seam without granting caller deletion authority — cost if wrong: one additional typed command surface to revise.
Task12 copy/fixture preparation e7a787bc: exact missing-table dependency graph and legal-at-insert corrections for two immutable receipt fixtures documented in task-12-copy-fixture-preflight.md. Read-only, no tests/runtime changes; final schema inventory remains after reviewed10c/11.
Pre-final hierarchy audit9286a1b0 PASS C0/I0 for dynamic descendant invalidation/wake and recursive root closure. Task11 ownership confirmed for canonical pr_merge guard, final-review route suppression, and project periodic merge sweep suppression; exact entrypoints in pre-final-hierarchy-audit.md carried into Task11 brief. This bounded read-only audit does not replace final branch review or Task12 E2E.
Ruling: Existing integration_seal now becomes optional with server-time default while preserving explicit trusted callers; sweep_due maps durable operation_id to request_id — the stored event has no clock and prose must not fabricate authority — cost if wrong: a backwards-compatible command default needs revision, without changing event schemas.

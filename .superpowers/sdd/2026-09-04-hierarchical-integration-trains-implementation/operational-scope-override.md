# Approved operational scope — 2026-09-06

User: "Skip the security checks and recover tests, and final branch review" followed by "Ok close the remaining gap on operational work". This supplement supersedes conflicting requirements in Task11/11b/11c/11d/12 and the original design/plan for this delivery.

- Complete operational backend Task11c and CLI/doctor/guide Task11d, with focused tests and scoped phase reviews. No whole-branch review or broad recovery/crash/isolated-daemon matrix.
- Defer Task11b protection inspection, positive/negative scratch probes, transport/worker/control-plane isolation certification and fingerprint wiring. Do not implement probe commands or fabricate evidence. Existing reviewed authentication, expected-OID checks, exact-CI proof, credential transport and irreversible-write safeguards remain intact.
- Functional enablement preflight still checks typed policy/artifacts/routes, required checks/producer configuration, repository identity/origin and actual provider/playbook wiring. Deferred certification is explicitly `not_performed` in status/guide, not a permanent blocker for the user-approved operational rollout. Replace the hardcoded preflight_evidence_unavailable placeholder with actual functional readiness; never report security certified.
- Task11c starts from reviewed11a, not11b. Its existing brief and entrypoint map apply except the deferred certification/probe requirements above. Do not read the obsolete whole Task11 brief or require a11b report. Read task-11a-report.md for APIs. Drop broad PostgreSQL recovery/race gates; focused control behavior tests remain required.
- Task11d starts after reviewed11c. Commands are status, flush, enable, waive-history, resume, abort, retry-cleanup. Doctor reports operational state/config/schema/draining without security certification or automatic fixes. Guide documents actual configuration, operator-only schema upgrade, restart, observe/hierarchy/train, controls, drain/rollback, and explicitly deferred certification/recovery verification. Do not require missing probe tooling in the documented rollout sequence.
- No production enablement, operator database/config mutation, deployment, push, PR, main merge, or new workspace. Same workspace only. Use apply_patch; exact owned-path commits. No full suite, no upward test workers; aq test for multiple files. No nested subagents.
- Unfinished11b transport edits and two tests were preserved under deferred-11b/ (transport.patch and test source .txt files), then removed from active source/test collection; reviewed HEAD transport remains intact. These artifacts permit resuming deferred work later.
- SQLite-to-PostgreSQL migration-copy compatibility is assessed separately; do not silently claim that path ready or resume the whole Task12 matrix.

Ruling: Deferred certification is not an enablement blocker in this operational delivery, following the user's accepted scope. Cost if wrong: rollout may encounter protection/isolation incompatibility not caught before activation; status and guide must expose the absence of certification, and no actual deployment is performed here.

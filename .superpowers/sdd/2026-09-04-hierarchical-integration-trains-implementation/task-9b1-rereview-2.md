### Finding Verdicts

1. **Candidate Git writes are not protected by current lease, revision, or branch-owner authority.** — **NOT ADDRESSED (Critical).** The fix now snapshots and revalidates the operation episode and active stage (`src/integration/candidates.py:1439-1443`), makes pending insertion race-safe (`src/integration/candidates.py:1292-1335`), and gives the conflict transition affected-row-checked member/batch CASes (`src/integration/candidates.py:1723-1764`). The new invalidator protocol is still not atomic, however. `rebuild()` checks `_has_live_mutation()` on an unlocked read connection at `src/integration/candidates.py:252-258`, performs several Git/forge awaits, and its eventual hierarchy transaction at `src/integration/candidates.py:295-343` never rechecks the mutation table. A concurrent build can reserve and commit an exact mutation claim after line 252 and begin its external push; rebuild can then supersede that revision underneath it. The submitted test pre-inserts the claim before calling rebuild (`tests/test_integration_candidates.py:901-950`) and cannot exercise this race. In addition, claims last only 60 seconds (`src/integration/candidates.py:139`) and are capped at the project-lease expiry (`src/integration/candidates.py:1550-1571`), while the authorized Git transport may run for 120 seconds (`src/git/manager.py:336-338`). If either deadline passes during the push at `src/integration/candidates.py:1621-1631`, the remote can advance before post-I/O lease validation rejects reconciliation at `src/integration/candidates.py:1633-1641`; later `_locked_state()` refuses the expired lease (`src/integration/candidates.py:909-921`), leaving the remote-ahead claim without the promised recovery path. Make invalidation query/block the live claim inside the same hierarchy transaction that changes the revision, and make the lease/claim lifetime cover the bounded external operation or add an authoritative expired-lease reacquisition/reconciliation path; an expired writer must not initiate a push.

2. **Repair acceptance bypasses both the reserved-path boundary and Task7's writer authority.** — **NOT ADDRESSED (Critical, with an Important provenance sub-gap).** Exact task/session/instance/workspace-ID/repository/branch/fence/stage checks and the reservation-qualified repair ref are materially improved (`src/integration/candidates.py:755-830`), and repair publication can reconcile a crash after its separate repair-ref push. The repair-to-collector handoff is not itself replay-safe: `accept_repair()` durably transfers the old repair fence at `src/integration/candidates.py:619-632` before it reserves or applies the `repair_handoff` mutation at `src/integration/candidates.py:633-647`. A crash, cancellation, unexpected remote, or lost claim after the transfer leaves the resolution `pushed` but the branch owned by the collector. Fresh replay reconstructs the obsolete repair fence and calls `transfer()` again; `BranchOwnership._require_current()` fails before any canonical handoff reread (`src/integration/ownership.py:117-123`), and `accept_repair()` catches only `BranchBusy`, not `StaleFence` (`src/integration/candidates.py:626-631`). The same path also cannot complete a repaired current revision: after the partial ref is advanced to the accepted head, final publication still freezes `expected_old_sha` as the previous revision head (or zero for revision 0) at `src/integration/candidates.py:1139-1170`, so `_mutate_ref()` treats the actual accepted-repair head as unexpected and returns `wait` at `src/integration/candidates.py:1578-1589`. The new repair test jumps directly from acceptance to N+1 rebuild instead of resuming/building N (`tests/test_integration_candidates.py:1376-1393`), so it misses both failures. Model transfer plus handoff publication as one durable replay state that canonical-rereads current ownership and remote state, and derive final publication's immutable expected-old OID from the latest applied partial/handoff mutation for that revision.

   The exact reservation provenance is also incomplete (Important): the schema freezes `repair_workspace_id` but has no immutable workspace-path field (`src/database/tables.py:2110-2136`). Push authorization merely requires the current scope's path to be non-null while matching the persisted workspace ID (`src/integration/candidates.py:808-822`) and then uses that current path at `src/integration/candidates.py:708-716`. Because `workspace_path` is mutable state, rebinding the same workspace row and session work directory preserves every comparison while changing the checkout from which privileged Git publishes. Persist the normalized/real workspace path at reservation and compare it with the current server-owned scope before importing or pushing; test same-ID path rebinding, not only replacement workspace IDs.

3. **Accepted repair lineage is no longer replayed during rebuild.** — **ADDRESSED.** `_accepted_parent_repair()` resolves `accepted_reservation_id` through the normalized resolution table, requires exact accepted batch/parent-revision/ordinal identity, imports the exact accepted head, reconstructs the frozen lineage, and revalidates it (`src/integration/candidates.py:1670-1709`). The actual-Git regression verifies N+1 repaired content and unchanged source refs (`tests/test_integration_candidates.py:1382-1393`).

4. **Publication replay is not durably reconciled after the PR boundary.** — **ADDRESSED.** Both dialect guards make PR number/URL immutable once published and enforce adjacent/replay transitions (`migrations/versions/e1eab6dbc186_candidate_durable_mutation_claims.py:20-112`); provider output is structurally bound to the canonical GitHub repository, PR number, base/head refs, exact head, and idempotency key (`src/integration/candidates.py:1234-1256`); and a lost final CAS rereads and compares the canonical row (`src/integration/candidates.py:1257-1284`). The restart matrix constructs a fresh provider over a shared durable fake backing store after create-before-persist (`tests/test_integration_candidates.py:683-696`).

5. **The new exact-fetch broker can swallow cancellation during normal broker settlement.** — **ADDRESSED.** `_settle_app_credential_broker()` distinguishes caller cancellation via the current task's cancellation state, cancels/reaps the broker, and re-raises (`src/git/manager.py:3118-3139`). The bounded regression cancels specifically during settlement and proves the broker's `finally` ran (`tests/test_git_app_auth.py:55-79`).

6. **The fix holds database write transactions over forge/token/Git network operations.** — **ADDRESSED.** Token acquisition, authenticated ref reads/pushes, and forge lookup/create are now outside the short hierarchy transactions (`src/integration/candidates.py:1176-1189`, `src/integration/candidates.py:1220-1257`, `src/integration/candidates.py:1578-1633`). The instrumented test covers candidate token/ref/push/forge awaits at transaction depth zero (`tests/test_integration_candidates.py:851-899`). The remaining authority defects are the durable protocol failures in verdicts 1 and 2, not a retained network-under-lock await.

### New Breakage in the Fix Diff

1. **Important — upgrade fabricates a repair-ref provenance that cannot replay a pre-upgrade pushed resolution.** Before this migration, repair publication pushed `resolved_head_sha` to the integration branch and stored no qualified target. Upgrade unconditionally backfills every existing resolution to `refs/heads/aq/integration-repairs/<id>` (`migrations/versions/e1eab6dbc186_candidate_durable_mutation_claims.py:193-220`). A valid pre-upgrade row in `state='pushed'` therefore names a ref that was never created; current `accept_repair()` checks that fabricated ref and returns stale (`src/integration/candidates.py:569-583`). The migration test round-trips only empty tables (`tests/test_migration_candidate_mutation_claims.py:65-116`), so it does not cover this live-row case. Preserve the actual legacy target provenance with a compatible representation, or add an explicit pre-DDL drain/guard for pushed resolutions and seed that case in both dialect tests. Separately, the backfill copies nullable stage deadlines and immediately makes them non-null (`migrations/versions/e1eab6dbc186_candidate_durable_mutation_claims.py:211-225`); the same preflight should give a deliberate diagnostic for any structurally valid legacy row that cannot be reconstructed rather than failing accidentally at `ALTER`.

### Out-of-Scope Observations

- The SQLite-to-PostgreSQL copy adapter omission remains the pre-existing cross-phase gap recorded by the implementer; this diff does not make an already-supported integration-table copy path regress because the adapter does not yet enumerate any of the hierarchical-integration tables. **Task 12 must own adding the complete dependency-ordered Task7-Task9 table family, including `integration_candidate_ref_mutations`, before PostgreSQL copy/cutover is supported.**

### Test and Report Checks

- The fix report names focused regressions and records a final `aq test` result of 96 passed plus a two-dialect migration result of 2 passed. Those suites were not rerun, per the scoped re-review instruction.
- No focused test was run: the remaining failures follow directly from missing atomic predicates/state transitions, and the reported tests omit the identified interleavings/live-row seed.
- Report claims match the submitted files for changed paths, lint, migration head, and the transaction-depth test. The claims that rebuild honors every live mutation and that handoff/expired claims are fresh-instance recoverable are not supported by the implementation or tests above.

### Quality Verdict

The fix establishes a useful shared mutation-claim abstraction, exact operation/stage checks, normalized repair lineage, and substantially stronger PR/cancellation handling. Its authority lifecycle is not yet closed across rebuild invalidation, lease expiry, or the repair-owner-to-collector boundary, and migration compatibility does not preserve existing pushed repair provenance.

### Verdict

**Fix round: Findings remain open.** Open counts: **Critical 2, Important 2, Minor 0**. Original findings 1 and 2 remain open; findings 3-6 are addressed. The migration compatibility issue is new breakage in this fix diff.

# Pre-final hierarchy readiness audit

## Verdict

**PASS for the three audited Tasks 1–10b seams: 0 Critical, 0 Important prior-phase gaps.** The production paths preserve dynamic-child invalidation/wake and recursive root closure. The legacy merge paths below remain reachable while legacy policy is active, but they are the explicit, still-unfinished Task 11 cutover work; they must be closed atomically before any project enters `hierarchy` or `train` mode.

This is bounded early final-review evidence, not a reapproval of the whole branch. I did not inspect the in-progress Task 10c files, run tests, or perform network/operator writes.

## Audited seams

### 1. Dynamic descendant creation during parent verification — PASS

- The real session filing route resolves the held parent's clean, canonical, exactly pushed workspace HEAD before creating a child (`src/commands/task_commands.py:1942`, `src/commands/task_commands.py:1955`; `src/integration/hierarchy.py:86`). Branchless verifier and repair delegates additionally carry their exact live operation/session-instance/workspace/fence binding into the filing transaction (`src/commands/task_commands.py:1554`, `src/commands/task_commands.py:1563`, `src/commands/task_commands.py:1275`).
- The authoritative write takes the project hierarchy lock, compares the expected generation when supplied, increments the generation once, and clears `verified_sha`/`verified_generation` before storing the immutable immediate-parent repository/ref/base origin (`src/integration/hierarchy.py:357`, `src/integration/hierarchy.py:364`, `src/integration/hierarchy.py:370`, `src/integration/hierarchy.py:393`, `src/integration/hierarchy.py:1049`). A child is structurally attached to that immediate parent in the same transaction (`src/integration/hierarchy.py:381`).
- Readiness re-enumerates every current direct child and requires an exact receipt/disposition for each (`src/integration/parent_completion.py:392`, `src/integration/parent_completion.py:484`). A newly ready generation emits an operation- and generation-stable event carrying the exact persisted verifier and repository-qualified target/fence (`src/integration/parent_completion.py:340`, `src/integration/parent_completion.py:343`, `src/integration/parent_completion.py:350`). The transfer command re-resolves that operation and then invokes `wake_verifier` for its real parent (`src/commands/integration_commands.py:198`, `src/commands/integration_commands.py:221`); `wake_verifier` rechecks readiness and the exact persisted owner/role/fence before waking only `operation.verifier_task_id` (or the parent itself) (`src/integration/parent_completion.py:940`, `src/integration/parent_completion.py:953`, `src/integration/parent_completion.py:963`, `src/integration/parent_completion.py:974`).
- A stale verification cannot complete the parent: completion re-locks the same hierarchy context and rejects a changed generation, readiness/head, verification row, operation, or verifier ownership (`src/integration/parent_completion.py:1011`, `src/integration/parent_completion.py:1015`, `src/integration/parent_completion.py:1017`, `src/integration/parent_completion.py:1026`, `src/integration/parent_completion.py:1033`, `src/integration/parent_completion.py:1046`). A completed parent also cannot acquire a later child (`src/database/queries/hierarchy_queries.py:598`).

The plan's production-path root → child → grandchild plus file-during-verification scenario remains correctly assigned to Task 12 as end-to-end evidence (`docs/superpowers/plans/2026-09-04-hierarchical-integration-trains-implementation.md:402`, `docs/superpowers/plans/2026-09-04-hierarchical-integration-trains-implementation.md:404`); no missing runtime seam was found here.

### 2. Recursive delivered closure and root-only sweep — PASS

- Sealing runs in one immediate transaction under the project hierarchy lock and derives members through the sole production `_eligible_members` → `eligible_root_page_on` path (`src/integration/scheduler.py:198`, `src/integration/scheduler.py:201`, `src/integration/scheduler.py:317`, `src/integration/scheduler.py:478`, `src/integration/scheduler.py:489`).
- The selector structurally excludes every child and grandchild with `tasks.parent_task_id IS NULL` (`src/database/queries/integration_train_queries.py:189`). A leaf must have no children and no parent episode/current verification; a root with children must instead name the current generation/head, current completed verification, exact parent completion, and completed parent operation (`src/database/queries/integration_train_queries.py:69`, `src/database/queries/integration_train_queries.py:70`, `src/database/queries/integration_train_queries.py:76`, `src/database/queries/integration_train_queries.py:78`, `src/database/queries/integration_train_queries.py:82`, `src/database/queries/integration_train_queries.py:93`). The selected root then needs the latest exact approved review, and parent review evidence must name that current verification (`src/integration/scheduler.py:498`, `src/integration/scheduler.py:506`, `src/integration/scheduler.py:513`, `src/integration/scheduler.py:521`).
- Recursive closure follows inductively through the production completion boundary: each non-leaf parent can complete only after its own direct children have exact receipts/dispositions and current aggregate verification, while generic task completion is rejected for any active managed parent (`src/integration/parent_completion.py:370`, `src/integration/parent_completion.py:1005`; `src/database/queries/task_queries.py:967`). Structural mutation below a sealed root is also guarded over both descendants and ancestors (`src/database/queries/hierarchy_queries.py:350`, `src/database/queries/hierarchy_queries.py:363`, `src/database/queries/hierarchy_queries.py:376`, `src/database/queries/hierarchy_queries.py:392`). Thus a nested parent contributes to a root only after its own descendant closure is complete, and no child is separately swept as a root.

### 3. Legacy merge bypasses — mandatory Task 11 activation blockers

These are not regressions in approved Tasks 1–10b; Task 11 explicitly owns them (`.superpowers/sdd/2026-09-04-hierarchical-integration-trains-implementation/task-11-brief.md:37`, `.superpowers/sdd/2026-09-04-hierarchical-integration-trains-implementation/task-11-brief.md:196`). They are concrete production entrypoints Task 11 must guard:

1. **Canonical mutation entrypoint:** `GitCommandsMixin._cmd_pr_merge` resolves a project and then proceeds directly through PR validation/CI to `GitManager.amerge_pr` without checking `hierarchical_integration_mode` (`src/commands/git_commands.py:24`, `src/commands/git_commands.py:62`, `src/commands/git_commands.py:85`, `src/commands/git_commands.py:99`, `src/commands/git_commands.py:109`). This one command is the only production caller of `amerge_pr` (`src/git/manager.py:2518`). Task 11 must reject it for `hierarchy|train` **immediately after the project read and before any forge/CI work**, irrespective of local/elevated/final-reviewer caller; the authenticated root-main promotion protocol uses its separate command and must not be represented as an exception supplied by `pr_merge` arguments.
2. **Final-review route and authority:** the global default pipeline creates a final-reviewer for every completed task carrying branch/PR data and creates downstream `pr-merged` gates (`src/prompts/default_playbooks/default-pipeline.md:125`, `src/prompts/default_playbooks/default-pipeline.md:132`, `src/prompts/default_playbooks/default-pipeline.md:149`). The shipped final-reviewer is instructed to invoke `pr_merge` (`src/profiles/defaults/final-reviewer/profile.md:91`), and the API explicitly grants that command after resolving the reviewed branch (`src/api/scope.py:312`, `src/api/scope.py:613`, `src/api/scope.py:633`). Task 11 must suppress this legacy route per enabled project and make the command-level guard authoritative, rather than relying on profile prose or global pipeline disablement.
3. **Periodic merge-sweep route:** the project playbook is enabled and its timer route creates/routes a `pr-merger` task (`src/prompts/project_playbooks/agent-queue/pr-merge-sweep.md:1`, `src/prompts/project_playbooks/agent-queue/pr-merge-sweep.md:6`, `src/prompts/project_playbooks/agent-queue/pr-merge-sweep.md:24`, `src/prompts/project_playbooks/agent-queue/pr-merge-sweep.md:32`); the profile exposes `pr_merge` (`src/profiles/defaults/pr-merger/profile.md:19`). Task 11 must disable/suppress this route for the migrated project in the same cutover CAS.

The daemon's `pr-merged` gate sweep is an observer, not another merge writer (`src/orchestrator/core.py:2617`, `src/orchestrator/core.py:2631`, `src/orchestrator/core.py:2644`). In accordance with the Task 11 ruling, existing historical gate rows should remain immutable and be handled through recorded applicability/waiver; they must not be resolved or deleted merely to perform cutover (`.superpowers/sdd/2026-09-04-hierarchical-integration-trains-implementation/task-11-brief.md:130`, `.superpowers/sdd/2026-09-04-hierarchical-integration-trains-implementation/task-11-brief.md:235`).

## Ownership summary

- **Prior Tasks 1–10b:** no Critical/Important correction identified in the audited seams.
- **Task 11:** owns all three legacy merge controls above; train/hierarchy activation is unsafe until the command guard and both per-project routing suppressions commit atomically with cutover.
- **Task 12:** owns the real-service end-to-end proof for dynamic file → stale close → deliver → exact re-wake/reverify and recursive root sealing; this audit found no behavior that needs to be deferred to Task 12 as a fix.

### Finding Verdicts

1. **Candidate Git writes are not protected by current lease, revision, or branch-owner authority.** — **NOT ADDRESSED (Critical).** Hierarchy-first resolution, lease/fence snapshots, collector acquisition, and most progress CASes are now present (`src/integration/candidates.py:657-831`, `src/integration/candidates.py:1143-1226`). However, the conflict transition still writes the member and shared batch lifecycle without `_validate_authority_on`, a current-revision predicate, or affected-row checks (`src/integration/candidates.py:1253-1268`). A rebuild or stage/lease/owner change after `merge-tree` can therefore leave an obsolete revision's conflict row while resetting the current batch to `repairing`. `_validate_authority_on` also never compares the live `operation.active_stage` with the snapshotted stage (`src/integration/candidates.py:1177-1203`), so `_conflict` can persist and dispatch the stale stage captured at `src/integration/candidates.py:1241-1248`. Finally, collector ownership is re-entrant for the same operation (`src/integration/ownership.py:97-103`), while `_pending` checks outside its transaction then unconditionally inserts (`src/integration/candidates.py:1087-1104`); duplicate concurrent build activations can still race into a raw uniqueness failure. Make the conflict transition a hierarchy-first, current batch/revision/stage/lease/fence CAS, include exact operation episode/current stage in every revalidation, and add an operation-scoped construction exclusion or transactional idempotent reread for same-owner duplicates.

2. **Repair acceptance bypasses both the reserved-path boundary and Task7's writer authority.** — **NOT ADDRESSED (Critical).** Raw caller lineage and both reserved-path ranges are now rejected, the stage is no longer always hardcoded to zero, exact repair objects are App-auth fetched, and handback uses `BranchOwnership.transfer` (`src/integration/candidates.py:488-606`, `src/integration/candidates.py:537-555`). The actual reserve/push authority remains incomplete: `reserve_repair` releases `_locked_state` and then files without the hierarchy lock or an atomic recheck of batch/current revision/conflict/fence (`src/integration/candidates.py:390-470`); `push_repair` checks the caller only against the current scope, but never compares that scope/principal's operation, task, session, instance token, workspace, repository, branch, or supplied fence target to the immutable reservation (`src/integration/candidates.py:608-640`). A replaced instance or another same-task/same-token-number fence can therefore push a reservation it did not author while locking a different branch. A crash after that remote push rolls back `state='pushed'`; replay blindly retries with `expected_old_oid=partial_head_sha`, which fails because the remote already equals `resolved_head_sha` (`src/integration/candidates.py:632-654`). Conflict publication has analogous recovery gaps: DB conflict is committed before the partial push, and replay of an existing conflict dispatches immediately without reconciling/publishing the partial (`src/integration/candidates.py:842-854`, `src/integration/candidates.py:1253-1292`); on N+1 it accepts only absent/already-partial remote state and cannot lease-replace the prior revision's head. Revalidate the exact reservation against the server-owned current scope and all persisted session/workspace/fence fields under hierarchy order, lock the reservation's exact target, reconcile partial/resolved remote tips before retry, and durably model/reconcile partial publication for initial and rebuilt conflicts before dispatch.

3. **The non-empty production publication path can silently skip publication and bypass Task9a authentication/repository binding.** — **ADDRESSED.** Missing App/forge dependencies return `configuration_blocked` before revision creation (`src/integration/candidates.py:167-184`); production repository URL is checked against the App binding (`src/integration/candidates.py:1454-1460`); source/base objects use exact-OID App-auth import only (`src/integration/candidates.py:1476-1502`); exact remote refs use the numeric-repository App API (`src/git/github_app.py:166-185`); and candidate pushes remain bound to `GitHubAppClient.repository`. The retained store is initialized locally without a remote or wildcard fetch (`src/integration/candidates.py:1462-1474`).

4. **Publication replay is not durably reconciled after the PR boundary.** — **NOT ADDRESSED (Important).** The new per-revision intent and monotone states materially improve ref replay (`src/integration/candidates.py:950-1037`), and every activation rechecks the authenticated remote ref. But the purported immutable publication identity excludes `pr_number` and `pr_url` in both dialect triggers (`migrations/versions/69416e65ee21_candidate_publication_authority.py:35-55`, `migrations/versions/69416e65ee21_candidate_publication_authority.py:88-113`), so a published PR can be rebound while remaining structurally valid. Replay ignores a zero-row final CAS (`src/integration/candidates.py:1060-1083`) and returns the provider's values without comparing them with the persisted canonical PR; the provider DTO also does not prove that `url` encodes the claimed repository and number (`src/integration/candidates.py:51-61`, `src/integration/candidates.py:1051-1059`). The crash matrix still reuses one in-memory `_AuditForge` result across service instances (`tests/test_integration_candidates.py:333-354`, `tests/test_integration_candidates.py:644-710`), so create-before-persist is not tested with a fresh provider. Make PR number/URL immutable after first publication, enforce exact adjacent transitions or exact replay identity, read and compare the canonical row when CAS affects zero rows, validate URL/repository/number coherence, and prove lookup-after-create with a newly constructed provider.

5. **Candidate/rebuild Git inputs are not the sealed authoritative inputs.** — **ADDRESSED.** Construction now proves `source_base_sha` ancestry and uses it as the explicit merge base (`src/integration/candidates.py:855-878`, `src/integration/candidates.py:1364-1376`), with actual-Git coverage for divergent history plus binary/delete/rename deltas (`tests/test_integration_candidates.py:1104-1205`). Rebuild reads the authenticated default ref, exact-fetches/verifies it, reads it again, and leaves N current on mismatch before entering the supersession transaction (`src/integration/candidates.py:249-287`, `tests/test_integration_candidates.py:1208-1241`).

### New Breakage in the Fix Diff

1. **Critical — accepted repair lineage is no longer replayed during rebuild.** Acceptance now replaces `conflict_evidence` with only `{"accepted_reservation_id": ...}` (`src/integration/candidates.py:561-575`), but `_accepted_parent_repair` still looks exclusively for an embedded `accepted_lineage` object (`src/integration/candidates.py:1228-1239`). N+1 consequently reapplies the raw sealed member, either losing accepted resolution content or reopening an avoidable conflict. The previous rebuild-preserves-repair test was removed rather than migrated to the normalized reservation. Resolve the reservation ID to immutable accepted lineage when rebuilding and restore the N/N+1 actual-Git preservation test.

2. **Important — the new exact-fetch broker can swallow cancellation during normal broker settlement.** After the Git leader exits, the fetch path awaits `_settle_app_credential_broker` outside its exception cleanup path (`src/git/manager.py:3041-3044`). That helper catches any `CancelledError`, including cancellation of the caller rather than cancellation of the broker task (`src/git/manager.py:3118-3128`); cancellation arriving at that await is converted into `served=False` and then a `GitError` at `src/git/manager.py:3058-3059`. Preserve caller cancellation separately while still cancelling/reaping the broker, and add a bounded cancellation-at-settlement test for the new fetch path.

3. **Important — the fix now holds database write transactions over forge/token/Git network operations.** Candidate and conflict publication hold `db.immediate()`, the hierarchy lock, and the ownership row while awaiting an App ref read and authenticated Git push (`src/integration/candidates.py:996-1025`, `src/integration/candidates.py:1274-1291`). Repair push additionally mints/refreshes the installation token inside that transaction before the Git push (`src/integration/candidates.py:615-640`). These calls can consume the provider/Git timeouts and block all SQLite writers or long-hold PostgreSQL row/advisory locks, contrary to the task's no-long-network-transaction constraint. Mint credentials before acquiring mutation exclusion, and use a durable fenced mutation intent/reconciliation protocol that preserves exact authority through the write without holding the general database transaction across network latency.

### Out-of-Scope Observations

None. Static migration inspection confirms `69416e65ee21` directly follows the existing merge head `9b3e5a7c1d20`, adds only Task9b1 publication/resolution authority tables, uses RESTRICT authority FKs, guards live-row downgrade, and has no Task9b2 root-promotion intent. The reported SQLite/PostgreSQL migration gates were not rerun.

### Verdict

**Fix round:** Findings remain open — original Critical findings 1 and 2, original Important finding 4, plus three fix regressions (one Critical, two Important). Open counts: **Critical 3, Important 3**.

No tests were run; the unresolved scenarios are explicit missing comparisons/transitions in the submitted fix, and the report already records the relevant focused and migration gates.

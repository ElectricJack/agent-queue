<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `report_brief`

**Report Brief.** Read a bounded, paged report brief and its CAS version.

| Property | Value |
|---|---|
| Capability | `report_brief` |
| Side effect | `read` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/report.py`](../../../src/commands/contracts/report.py) |
| Contract fingerprint | `sha256:4c8a74041025367cf8a0ab99891e930fcd5838256c01d968bda53fa1c9949e5f` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `request_id` | `string` | yes | — | — |
| `offset` | `integer` | no | `0` | — |
| `limit` | `integer` | no | `20` | — |

## Result

| Field | Type | Description |
|---|---|---|
| `request_id` | `string` | — |
| `state` | `string` | — |
| `deadline` | `number` | — |
| `version` | `integer` | — |
| `brief_hash` | `string` | — |
| `brief` | `object` | — |
| `facts` | `object[]` | — |
| `active` | `object[]` | — |
| `total_facts` | `integer` | — |
| `total_active` | `integer` | — |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `completed` | success | Completed |
| `rejected` | failure | Rejected |

## Declared effects

This command declares no effect clause, so the playbook graph falls back to its side-effect class and argument names:

| Effect | Subject | Condition |
|---|---|---|
| Read using request_id, offset, limit | — | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

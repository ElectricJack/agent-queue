<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_promote_main`

**Promote exact root candidate.** Reconcile and fast-forward main to the exact trusted green candidate.

| Property | Value |
|---|---|
| Capability | `integration_promote_main` |
| Side effect | `composite` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:59478dc25e293d029e68a96644dbda7633408f23c6456ee9d301b9faaae496ed` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `batch_id` | `string` | yes | — | — |
| `revision` | `integer` | yes | — | — |

## Result

| Field | Type | Description |
|---|---|---|
| `batch_id` | `string \| null` | — |
| `revision` | `integer \| null` | — |
| `intent_id` | `string \| null` | — |
| `receipt_ids` | `string[]` | — |
| `head_sha` | `string \| null` | — |

Projected into the run receipt: `batch_id`, `revision`, `intent_id`, `receipt_ids`, `head_sha`.

Redacted in receipts and explanations: `head_sha`.

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `already_promoted` | success | — |
| `base_moved` | failure | — |
| `ci_missing` | failure | — |
| `configuration_blocked` | failure | — |
| `non_fast_forward` | failure | — |
| `promoted` | success | — |
| `reconciliation_blocked` | failure | — |
| `stale` | failure | — |
| `wait` | failure | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Update integration operation | `integration_operation` | always |
| Update delivery evidence | `delivery_evidence` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

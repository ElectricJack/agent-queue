<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_delivery_readiness`

**Integration Delivery Readiness.** Read whether every child of one parent has delivered, changing nothing.

| Property | Value |
|---|---|
| Capability | `integration_delivery_readiness` |
| Side effect | `read` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:e7add026807c6424ceaa093d054ba2009d94a1a2cf2b4c7f2324c62907484f3f` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `task_id` | `string` | yes | — | — |

## Result

| Field | Type | Description |
|---|---|---|
| `task_id` | `string \| null` | — |
| `episode_id` | `string \| null` | — |
| `operation_id` | `string \| null` | — |
| `generation` | `integer \| null` | — |
| `checkpoint_sha` | `string \| null` | — |
| `head_sha` | `string \| null` | — |
| `receipts` | `object[]` | — |
| `blockers` | `object[]` | — |
| `required_checks` | `object \| null` | — |
| `on_failed_child` | `"block" \| "ask" \| null` | — |

Projected into the run receipt: `task_id`, `episode_id`, `operation_id`, `generation`, `checkpoint_sha`, `head_sha`, `receipts`, `blockers`, `required_checks`, `on_failed_child`.

Redacted in receipts and explanations: `checkpoint_sha`, `head_sha`.

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `failed` | failure | — |
| `invariant_error` | failure | — |
| `ready` | success | — |
| `waiting` | failure | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Read delivery evidence | `delivery_evidence` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

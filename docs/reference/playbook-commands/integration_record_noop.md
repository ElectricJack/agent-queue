<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_record_noop`

**Record verified no-code child disposition.** Bind a child's current no-op completion and exact Git head to its parent receipt.

| Property | Value |
|---|---|
| Capability | `integration_record_noop` |
| Side effect | `update` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:189c443a0b2897e3d37877f5a05f2767700cdd7f6fa4c4ec834e102615ff5672` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `child_task_id` | `string` | yes | — | — |
| `expected_head_sha` | `string` | yes | — | — |

Redacted in receipts and explanations: `expected_head_sha`.

## Result

| Field | Type | Description |
|---|---|---|
| `receipt_id` | `string \| null` | — |
| `revision` | `integer \| null` | — |
| `reviewed_head_sha` | `string \| null` | — |
| `reviewed_tree_sha` | `string \| null` | — |

Projected into the run receipt: `receipt_id`, `revision`.

Redacted in receipts and explanations: `reviewed_head_sha`, `reviewed_tree_sha`.

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `delivery_target_fixed` | failure | — |
| `invalid` | failure | — |
| `recorded` | success | — |
| `stale_head` | failure | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Update delivery evidence | `delivery_evidence` | always |
| Update task | `task` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `delivery_promote`

**Promote reviewed child delivery.** Prepare and lease-push one reviewed squash to its immediate parent.

| Property | Value |
|---|---|
| Capability | `delivery_promote` |
| Side effect | `composite` |
| Idempotency | keyed on `operation_key` |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:2b7c3be746819f7d011e3493f5db6b5ec87d52c141704fa273d5e6ff40e9ee31` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `operation_key` | `string` | yes | — | — |
| `source_task_id` | `string` | yes | — | — |
| `source_head` | `string` | yes | — | — |
| `source_base` | `string` | yes | — | — |
| `expected_target` | `string` | yes | — | — |
| `fence` | `Fence` | yes | — | — |

Redacted in receipts and explanations: `expected_target`, `fence`, `source_base`, `source_head`.

## Result

| Field | Type | Description |
|---|---|---|
| `intent_id` | `string \| null` | — |
| `receipt_id` | `string \| null` | — |
| `prepared_sha` | `string \| null` | — |

Projected into the run receipt: `intent_id`, `receipt_id`, `prepared_sha`.

Redacted in receipts and explanations: `prepared_sha`.

## Nested objects

### `BranchKey`

| Field | Type | Description |
|---|---|---|
| `repository_id` | `string` | — |
| `branch` | `string` | — |

### `Fence`

| Field | Type | Description |
|---|---|---|
| `target` | `BranchKey` | — |
| `owner_id` | `string` | — |
| `token` | `integer` | — |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `already_promoted` | success | — |
| `conflict` | failure | — |
| `promoted` | success | — |
| `source_moved` | failure | — |
| `target_moved` | failure | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Create or reuse integration operation keyed by "operation_key" | `integration_operation` | always |
| Update branch ownership | `branch_ownership` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_repair_start`

**Integration Repair Start.** Activate or durably continue one operation's bounded repair stage.

| Property | Value |
|---|---|
| Capability | `integration_repair_start` |
| Side effect | `composite` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:12e0c46747ee76cb977cd2515549598d296987400d6a138a01ac9561fe7750f9` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `operation_id` | `string` | yes | — | — |
| `starting_sha` | `string` | yes | — | — |
| `trigger_id` | `string` | yes | — | — |

Redacted in receipts and explanations: `starting_sha`.

## Result

| Field | Type | Description |
|---|---|---|
| `operation_id` | `string \| null` | — |
| `stage` | `0 \| 1 \| null` | — |
| `starting_sha` | `string \| null` | — |
| `started_at` | `number \| null` | — |
| `deadline_at` | `number \| null` | — |

Projected into the run receipt: `operation_id`, `stage`, `starting_sha`, `started_at`, `deadline_at`.

Redacted in receipts and explanations: `starting_sha`.

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `already_started` | success | — |
| `invariant_error` | failure | — |
| `stale` | failure | — |
| `started` | success | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Create or reuse integration operation keyed by "operation_id" | `integration_operation` | always |
| Update integration operation | `integration_operation` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

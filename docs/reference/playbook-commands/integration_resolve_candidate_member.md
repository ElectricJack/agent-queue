<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_resolve_candidate_member`

**Resolve candidate member.** Reserve, publish, accept, and continue the exact conflicted candidate member owned by the authenticated repair session.

| Property | Value |
|---|---|
| Capability | `integration_resolve_candidate_member` |
| Side effect | `composite` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:9e20b1a77bb04c8a4414e020c8b8dd9a8b7324e722683b9dc847ee0639edf91f` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `resolved_head_sha` | `string` | yes | — | — |
| `resolved_tree_sha` | `string` | yes | — | — |
| `repair_commit_shas` | `string[]` | yes | — | — |
| `claim_epoch` | `integer \| null` | no | `null` | — |
| `task_id` | `string \| null` | no | `null` | — |
| `session_id` | `string \| null` | no | `null` | — |
| `project_id` | `string \| null` | no | `null` | — |

Redacted in receipts and explanations: `repair_commit_shas`, `resolved_head_sha`, `resolved_tree_sha`.

## Result

| Field | Type | Description |
|---|---|---|
| `reservation_id` | `string \| null` | — |
| `batch_id` | `string \| null` | — |
| `revision` | `integer \| null` | — |
| `member_ordinal` | `integer \| null` | — |
| `partial_head_sha` | `string \| null` | — |
| `continuation` | `object \| null` | — |

Projected into the run receipt: `reservation_id`, `batch_id`, `revision`, `member_ordinal`.

Redacted in receipts and explanations: `continuation`, `partial_head_sha`.

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `accepted` | success | — |
| `already_accepted` | success | — |
| `invariant_error` | failure | — |
| `stale` | failure | — |
| `wait` | failure | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Update branch ownership | `branch_ownership` | always |
| Update integration operation | `integration_operation` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

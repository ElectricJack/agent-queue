<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `ensure_task`

**Ensure a task exists.** Create the task, or reuse the one already keyed by this deduplication key.

| Property | Value |
|---|---|
| Capability | `ensure_task` |
| Side effect | `create` |
| Idempotency | keyed on `dedup_key` |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/builtin.py`](../../../src/commands/contracts/builtin.py) |
| Contract fingerprint | `sha256:52852589dd1329aa5fa6eceb0406b9d7e5aa14c50eba09814a68687cf07463bb` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `dedup_key` | `string` | yes | — | Deduplication key |
| `title` | `string` | yes | — | Title |
| `project_id` | `string \| null` | no | `null` | Project |
| `description` | `string \| null` | no | `null` | Description |
| `priority` | `integer \| null` | no | `null` | Priority |
| `profile_id` | `string \| null` | no | `null` | Agent profile |
| `intelligence_class` | `string \| null` | no | `null` | Intelligence class |
| `initial_status` | `string \| null` | no | `null` | Initial status |
| `parent_id` | `string \| null` | no | `null` | — |
| `root` | `boolean \| null` | no | `null` | — |
| `reason` | `string \| null` | no | `null` | — |
| `discovered_from` | `string \| null` | no | `null` | — |

## Result

| Field | Type | Description |
|---|---|---|
| `task_id` | `string` | Task |
| `created` | `boolean` | Was created |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `created` | success | Created |
| `rejected` | failure | Rejected |
| `reused` | success | Reused |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Create or reuse a task keyed by "dedup_key" | `task` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

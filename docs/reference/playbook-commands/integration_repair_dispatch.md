<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `integration_repair_dispatch`

**Integration Repair Dispatch.** Create the repair task and hand the current branch writer fence to it.

| Property | Value |
|---|---|
| Capability | `integration_repair_dispatch` |
| Side effect | `composite` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/integration.py`](../../../src/commands/contracts/integration.py) |
| Contract fingerprint | `sha256:9bcd818cc23e3c0c35c9a898cf22fb976152de64e5b77cf837648f753de85550` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `operation_id` | `string` | yes | — | — |
| `stage` | `0 \| 1 \| null` | no | `null` | — |
| `batch_id` | `string \| null` | no | `null` | — |
| `revision` | `integer \| null` | no | `null` | — |
| `head_sha` | `string \| null` | no | `null` | — |

## Result

| Field | Type | Description |
|---|---|---|
| `operation_id` | `string \| null` | — |
| `stage` | `0 \| 1 \| null` | — |
| `repair_task_id` | `string \| null` | — |
| `writer_kind` | `"repair_delegate" \| "existing_verifier" \| null` | — |
| `fence` | `Fence \| null` | — |

Projected into the run receipt: `operation_id`, `stage`, `repair_task_id`, `writer_kind`, `fence`.

Redacted in receipts and explanations: `fence`.

## Nested objects

### `BranchKey`

| Field | Type | Description |
|---|---|---|
| `repository_id` | `string` | — |
| `branch` | `string` | — |

### `Fence`

| Field | Type | Description |
|---|---|---|
| `target` | `BranchKey` | — |
| `owner_id` | `string` | — |
| `token` | `integer` | — |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `already_dispatched` | success | — |
| `busy` | failure | — |
| `configuration_blocked` | failure | — |
| `dispatched` | success | — |
| `human_required` | failure | — |
| `stale` | failure | — |
| `writer_reused` | success | — |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Update integration operation | `integration_operation` | always |
| Update branch ownership | `branch_ownership` | always |
| Create or reuse task execution keyed by "operation_id" | `task_execution` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

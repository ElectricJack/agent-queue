<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `job_result`

**Job Result.** Read a job's immutable result and bounded excerpt.

| Property | Value |
|---|---|
| Capability | `job_result` |
| Side effect | `read` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/job.py`](../../../src/commands/contracts/job.py) |
| Contract fingerprint | `sha256:8ed1e7b2e72bb86d05b2bf53a2f10aeda227cca5244532b3622ecb4d8d2588e0` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `job_id` | `string` | yes | — | — |
| `max_bytes` | `integer` | no | `8192` | — |

## Result

| Field | Type | Description |
|---|---|---|
| `result` | `object \| null` | — |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `completed` | success | Completed |
| `rejected` | failure | Rejected |

## Declared effects

This command declares no effect clause, so the playbook graph falls back to its side-effect class and argument names:

| Effect | Subject | Condition |
|---|---|---|
| Read using job_id, max_bytes | — | always |

<!-- aq:generated:end -->

## Purpose

Read the immutable result, including actual outcome and a bounded failure-first excerpt.

The command delegates to `JobCommandsMixin` in
[`src/commands/job_commands.py`](../../../src/commands/job_commands.py).
Job state survives daemon restart in PostgreSQL; artifacts live under
`<data_dir>/runs/<job_id>/`. Scope checks deny other tasks and projects.

See the [durable wait guide](../../guides/agent-waits.md) for submission,
replay, deadlines, result consumption and installed grants.

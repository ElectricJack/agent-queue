<!-- aq:generated:start -->
<!-- Generated by scripts/gen-command-docs.py. Edit outside these markers only. -->

# `task_recovery_notify`

**Wake a task's recovery incident.** Record or reuse the one durable recovery incident for a blocked task and queue its single supervisor notice; a replayed failure reuses the same incident.

| Property | Value |
|---|---|
| Capability | `task_recovery_notify` |
| Side effect | `create` |
| Idempotency | natural |
| Retry safe | yes |
| Timeout | none |
| Preview | not supported |
| Defined in | [`src/commands/contracts/builtin.py`](../../../src/commands/contracts/builtin.py) |
| Contract fingerprint | `sha256:dc3672a4d2319f86d297c1f7bb7e0a0d34ad0977529959efdb6f066d26ab2429` |

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `task_id` | `string` | yes | — | Task |
| `project_id` | `string \| null` | no | `null` | Project |

## Result

| Field | Type | Description |
|---|---|---|
| `task_id` | `string \| null` | Task |
| `incident_id` | `string \| null` | Incident |
| `operation_id` | `string \| null` | Integration operation |
| `detail` | `string \| null` | Detail |
| `redelivered` | `boolean \| null` | Redelivered |

## Outcomes

| Outcome | Classification | Meaning |
|---|---|---|
| `existing` | success | Existing incident reused |
| `not_actionable` | success | Nothing to recover yet |
| `queued` | success | Incident queued |
| `rejected` | failure | Rejected |
| `retired` | success | Delegate retired |

## Declared effects

| Effect | Subject | Condition |
|---|---|---|
| Create the supervisor's incident notice | `message` | always |

<!-- aq:generated:end -->

## Purpose

TODO: what this command is for, in the reader's terms.

## When a playbook uses it

TODO: the situations a playbook step reaches for it.

## How it works internally

TODO: step by step through the executor and handler, with file references.

## Side effects and persistence

TODO: what it writes, which tables or files hold it, and what survives a restart.

## Failure modes and diagnostics

TODO: each failure outcome, what causes it, and the command that diagnoses it.

## Example step

TODO: a realistic playbook step that calls this command.

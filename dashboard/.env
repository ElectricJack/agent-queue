# The event stream defaults to the page's own origin, so `vite dev` reaches the
# daemon through the `/ws` proxy (`AQ_API_TARGET`) and a production build talks
# to whatever host serves it. Set VITE_WS_URL only to point a build at a fixed
# daemon, e.g. VITE_WS_URL=ws://127.0.0.1:8081.

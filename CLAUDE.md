# CLAUDE.md

Agent Queue — self-improving orchestration platform for AI coding agents. Manages task queues, coordinates multi-agent workflows via playbooks, accumulates knowledge through a 4-tier memory system, and continuously improves through automated reflection. The core value proposition: the system gets better with use — every task feeds the reflection engine, insights accumulate in scoped memory, and future agents benefit automatically. Discord + MCP + CLI controlled, SQLAlchemy-backed (PostgreSQL only), fully async Python.

## Quick Reference

- **Entry point:** `src/main.py` → orchestrator + Discord bot + embedded MCP server
- **Core files:** `orchestrator.py`, `src/commands/` (handler + mixin modules), `supervisor.py`, `database/` (including `src/database/queries/hierarchy_queries.py` — task hierarchy: `set_parent`, children/progress reads, close/delete/archive subtree semantics), `models.py`
- **Playbooks:** `src/playbooks/` — V2 only (`authoring`, `definition`, `pipeline_lowering`, `proposal`, `validation`, `activation`, `artifact_store`, `engine`, `runtime`, `run_state`, `routing`, `graph_projection`, `resume_handler`, `executors/`). The V1 compiler/runner/manager/store were deleted in the 2026-09-04 cutover; `tests/test_v1_removal.py` is the ratchet.
- **Memory:** External `aq-memory` plugin (install via `aq plugin install`), plus in-tree `facts_parser.py`, `profile_parser.py`
- **Profiles:** `src/profiles/` — `parser.py`, `sync.py`, `migration.py`, `project_override_migration.py`. Markdown source of truth in `vault/agent-types/<id>/profile.md`. Profiles are **global** — a durable worker is shared between projects, so there is one definition per agent type; project-scoped profiles (`project:<pid>:<id>`, `vault/projects/<pid>/agent-types/`) were retired, and `src/profiles/project_override_migration.py` promotes any left in a vault into their system profile (also `aq doctor --check profiles.project_overrides --fix`). The profile's `## Config.harness` field selects which CLI runs the agent (`claude`, `codex`, `gemini` — see `src/sessions/default_harnesses/`); **every** agent runs as a tmux session and `harness` is the only selector. The `runtime` and `agent_name` config keys were **removed** (with the in-process Supervisor and the `acpx` runtime respectively); `parser.py` rejects both with a pointer to `harness`. Shipped worker defaults use provider-explicit ids (`worker-<tier>-<level>-<provider>`: `worker-standard-medium-claude`, `worker-deep-high-claude`, `worker-fast-medium-claude`). `vault.ensure_default_profiles` is write-if-absent and skips ids tombstoned in `vault/agent-types/.retired-defaults` (`src/profiles/retired_defaults.py`, written by `aq agent delete-profile`, cleared by `aq agent profile-reseed`) so an operator's deletion survives a restart. Migration: `docs/guides/worker-pools.md` §7b.
- **Runtimes:** `src/runtimes/` — `base.py` (Runtime ABC, Capability enum, `requires_workspace` ClassVar) and the `RuntimeRegistry` in `__init__.py`. **There are no in-tree implementations**: every agent runs as a session (a CLI wrapped in tmux, chosen by `harness`), and the in-process Supervisor was deleted. `default_registry(config=...)` registers nothing; the registry remains the injection seam for tests and `sync_workflow`.
- **MCP registry:** `src/profiles/mcp_registry.py` (in-memory registry + vault watcher), `mcp_probe.py` (parallel probes, 10s timeout), `mcp_catalog.py` (cache), `mcp_inline_migration.py` (legacy extractor). Source of truth: `vault/[projects/<pid>/]mcp-servers/*.md`. Profiles reference servers by name.
- **Workspaces:** `src/orchestrator/workspace.py` + `src/orchestrator/workspace_attachments.py` (multi-kind acquisition), `src/database/queries/workspace_queries.py` + `workspace_kinds_queries.py` + `task_requirements_queries.py`. Tasks declare `requires_kinds` at creation; orchestrator atomically acquires one workspace per declared kind in canonical lock order. Kinds are markdown in `vault/[projects/<pid>/]workspace-kinds/<id>.md` (system + project override) — built-ins seeded by migration: `project-repo` (writable, exclusive lock), `vault` (auto-attached, no lock), `readonly-dir`. See `docs/specs/design/workspaces-v2.md`.
- **CI main sentinel:** `src/commands/ci_commands.py` (`ci_baseline_status` — read-only verdict on the default branch head via `gh`, failure signature, repair-task key and escalation) drives the project playbook `src/prompts/project_playbooks/agent-queue/ci-main-sentinel.md` (timer.15m → `ensure_task` a `deep-high` repair keyed `ci-baseline:<signature>:<attempt>`, human gate after two spent attempts). Reviewed bundle in `tests/fixtures/playbooks/v2/ci-main-sentinel/`; import + activate per install. Spec: `docs/superpowers/specs/2026-09-05-ci-main-sentinel-design.md`.
- **Blocked-task escalation:** system default playbook `src/prompts/default_playbooks/blocked-task-escalation.md` (`task.failed` filtered `status: BLOCKED` → one `message_send` to `session:supervisor-<pid>` telling the supervisor to read the session-log tail with `aq session logs` and decide retry / hold / follow-up / ask the human). `message_send` is contracted in `src/commands/contracts/builtin.py` for exactly this. Reviewed bundle in `tests/fixtures/playbooks/v2/blocked-task-escalation/`; import + activate per install. Spec: `docs/superpowers/specs/2026-09-06-blocked-task-escalation-design.md`.
- **Deleting a task that owns a branch:** `task_delete` takes `branches: keep|delete` (`aq task delete <id> --branches …`). In a hierarchy/train project, a subtree holding a materialized `task_branch_origins` row is refused with `hierarchy.branch_discard_required` until the caller says which — the refusal names the branches so a surface can ask (`dashboard/src/api/branchDiscard.ts` + `components/BranchDiscardPrompt.tsx`). Archive always keeps the branch. `discard` marks the retired origin `pending` and `src/integration/branch_discard.py` removes the ref asynchronously (CAS on the observed head, refuses a live branch owner or the default branch), parking conflicts for `aq doctor --check integration.branch_discards`. Revision `a00000000004` also narrows `task_branch_origin_materialized_immutable` so a materialized origin can be retired at all — identity and DELETE stay frozen. Spec: `docs/superpowers/specs/2026-09-08-task-deletion-with-materialized-branches-design.md`.
- **Recent work / model attribution:** `src/database/queries/activity_queries.py` (`list_recent_task_activity` — tasks touched in a time window, from `tasks`/`archived_tasks`, `task_completion_records` and the per-attempt `task_session_attempts` snapshot), command `task_recent_activity` (`aq task recent-activity`, `POST /api/task/recent-activity`). Models come from `task_session_attempts.model` and are never inferred from the profile; attempts with no model are counted as unattributed. Dashboard: the Tasks tab's *Time range* select (`window=` URL param) in `dashboard/src/pages/command-center/`. Spec: `docs/superpowers/specs/2026-09-07-recent-work-and-model-attribution-design.md`.
- **Discord digest (§8):** `src/digest/` — pure, clock-free layer: `facts.py` (`WorkFact`/`ActiveTask`/`DigestInputs`, stable fact keys), `eligibility.py` (the §8 send/silence table plus dedup of already-reported keys and repeated highlight wording), `render.py` (one message, ≤1,200 chars, mentions neutralised, overflow folded into a count), `aggregate.py` (`build_digest` — the single entry point shared by the dashboard's dry preview and real delivery), `dispatch.py` (`DigestScheduleService` — the only clock-owning half: it reserves the grid-aligned window row *before* delivery so a second daemon or a restart loses the insert instead of posting twice, persists a suppressed window as durably as a sent one, coalesces an outage into one labelled catch-up, and pumps deliveries on a separate lease that stands down entirely while any escalation delivery is owed a send or the rate guard is hot). Ticked from the orchestrator cycle (`Orchestrator.digest_schedule`), wired in `src/main.py`. The read half is `src/database/queries/digest_queries.py` (`collect_digest_activity`): durable evidence only — completions, attempt starts, task comments as progress notes, `pr_url` as a `vcs` milestone — and "active" means a live non-stale running attempt, never `updated_at` churn, a heartbeat, a stalled session or a worker parked on a human answer. Spec: `vault/projects/agent-queue/specs/discord-simplification-implementation.md` §8.
- **Formulas:** `src/task_graph/formulas.py` (registry, `extends` merge, vars), `src/commands/formula_commands.py` (`formula_list|show|cook`), provenance in `creator.write_plan`. Files: `vault/[projects/<pid>/]formulas/<name>.md` (frontmatter + one `aq-graph` block). Spec: design spec Part III.
- **Swarm (claims/pools):** `src/database/queries/claim_queries.py` (claim transaction, epoch fence), `src/commands/claim_commands.py` (`task_claim`), `src/claim_file.py` (leaf module owning the `.aq/claim.json` helpers — import these from here, never from `src.commands.claim_commands`, or `src.sessions` and `src.commands` form an import cycle), `src/orchestrator/pools.py` (`_measure_pools` → `PoolMeasurement`, `_reconcile_pools`; pure `size_pools` + `place_pool_actions` in `scheduler.py`), pool carve-outs in `src/sessions/reconciler.py`, checks in `src/doctor/pool_checks.py`. Profiles with `lifecycle: pool` pull work via `aq task claim`; `lifecycle: task` keeps push. Off by default (`swarm.enabled`). Sizing is **global per profile** — `PoolKey` is just `profile_id`, bounds/supply/demand are fleet-wide sums, ceilinged by `swarm.global_max_active` (default `resources.max_concurrent_agents`) — and a separate pure placement step picks which project each start lands in (quarantine, workspace capacity, `project.max_concurrent_agents` and the `min_per_project` warm floor are placement inputs; unplaceable starts surface as `pool.placement_starved`). `aq pool status` is one row per profile with a nested `projects[]` breakdown. The §10 work query is two statements — the frontier restricted to tasks pinned to the claiming agent, then the frontier at large — each ordered by `priority, created_at` alone so the three `idx_tasks_claim_frontier*` indexes make it an index-ordered scan that stops at the first admissible row; the affinity `CASE` that used to lead the `ORDER BY` could not come from an index and made every `LIMIT 1` read and sort the whole frontier. Specs: `docs/superpowers/specs/2026-08-28-swarm-work-model-design.md` Part II, re-scoped by `docs/superpowers/specs/2026-09-08-global-worker-pools-design.md`, plus `docs/superpowers/specs/2026-09-09-claim-frontier-ordered-scan-design.md`. Operator guide: `docs/guides/worker-pools.md`.
- **Resource gating:** `src/resources/` — `limits.py` (per-session env caps + `nice` + optional cgroup scopes, applied in `src/sessions/spec.py:_build`), `semaphore.py` (the box-wide `flock` slot semaphore behind `aq test`), `procs.py` (attribute load back to sessions). Config section `resources:`; checks in `src/doctor/resource_checks.py`; CLI in `src/cli/test_runner.py`. Guide: `docs/guides/resource-gating.md`.
- **Fleet metrics:** `src/metrics/sampler.py` (`MetricsSampler` — one sample/second, 1m/1h roll-ups, per-tier retention), `src/database/queries/metrics_queries.py`, `metrics_samples` table, `metrics.tick` on the bus (never `log_event`), `GET /api/metrics/series` (`src/api/metrics.py`), dashboard tab at `dashboard/src/pages/metrics/` (uPlot). Config section `metrics:`. Spec: `docs/specs/design/fleet-metrics.md`.
- **Escalation delivery (§7):** `src/escalations/` — pure `facts`/`render`/`plan` plus `transport` (the narrow port, its fault taxonomy and the `SinkTransport` every test uses) and `dispatch` (`EscalationDeliveryService`: reconcile open incidents into the `escalation_deliveries` outbox, lease a row, send once, reconcile an ambiguous send by its embedded `aq-esc:<dedup key>` marker, record the receipt). One root post plus one thread per incident; identity is the row's unique `dedup_key` and the stored channel/message/thread IDs, so replayed events, a reconnect or a restart never produce a second post. Ambiguity that cannot be reconciled is recorded `unknown`, never re-posted; a deleted root/thread earns at most one pending replacement generation and a closed incident earns none. Mentions come only from `discord.escalation.mention_user_ids`/`mention_role_ids` and only on the initial root — every interpolated string is neutralised. Discord side in `src/discord/escalation_transport.py`; ticked from the orchestrator cycle and wired in `src/main.py`. The **inbound** half is `src/escalations/intake.py` (pure `classify_inbound`) plus `src/discord/escalation_intake.py`, called first from `bot.on_message`: a thread message correlates only through `db.find_escalation_by_thread` (the confirmed receipt on the incident's root delivery — never a task-thread heuristic), only in the configured `discord.channel_id`, only from `discord.authorized_users`, and never from a bot. Identity comes from the gateway, handed to `escalation_reply` as `ExecutionPrincipal.service("discord:<id>")` → `human:discord:<id>`; no body field is ever trusted. The path may not resolve gates, mutate tasks or type into a session. A late reply to a terminal incident is recorded, queues no supervisor work, and gets closed-state guidance (the thread is re-archived after). Guide: `docs/guides/escalations.md`.
- **Discord messaging settings / digest preview:** `discord.channel_id` plus the nested `discord.digest` / `discord.escalation` settings in `src/config.py` (bounds: interval 15–1440 min, catch-up 1–168 h, snowflake IDs; digest and escalation enablement are independent). `src/digest/schedule.py` turns those settings into the durable `(destination, config_generation)` a `digest_windows` row is keyed by — a window-defining change starts a new generation. Commands `digest_preview` (dry, writes nothing) and `digest_status` (next evaluation + delivery health) in `src/commands/digest_commands.py`; dashboard panel and escalation inbox at `dashboard/src/pages/settings/Messaging.tsx` + `EscalationInbox.tsx` (Settings → Messaging). Cutover ledger: `docs/guides/discord-replacement-checklist.md`.
- **Default tuning:** `src/config_tuning.py` — `MachineResources.detect()` plus `recommended_tuning()`: the curated, resource-aware config sections a fresh install gets, derived from cores and RAM alone (no operator projects, memory or paths). One `TuningNote` per emitted key carries the rationale and override, and `tests/test_config_tuning.py` fails if a key has no note. Written by `aq system config tune --apply` (offline — it edits the YAML directly, so it works before the daemon exists) and by `src/setup_wizard.py`. Guide: `docs/guides/default-tuning.md`.
- **Config editor:** `src/config_editor.py` — ruamel round-trip writer behind `get_config` / `update_config` / `get_config_schema`. Validates via temp-file `load_config()` before swap.
- **Intelligence:** `prompt_builder.py`, `tools/registry.py`, `llm_logger.py`. Reflection is a playbook, not a module.
- **Intelligence classes:** `src/intelligence_classes/` — `__init__.py` (parser + `load_intelligence_classes`), `registry.py` (`IntelligenceClassRegistry`, a live `Mapping` kept current by the shared vault watcher over `vault/intelligence-classes/*.md`), `editing.py`. The orchestrator owns one registry (`Orchestrator.intelligence_classes`) and hands the *same object* to `SessionSpecBuilder`, so adding a class file makes it launchable with no daemon restart. A malformed file keeps the previous entry and is reported by `aq doctor --check intelligence_classes.parse`; `aq system reload-config` forces a rescan and `aq system list-intelligence-classes` shows `loaded` per class.
- **LLM direct path:** `src/llm/` — `LLMClient.complete`/`run_tools`, `LLMCallSpec`, config `llm:`, intelligence classes shared with sessions. Consumers: playbook nodes/transitions, plugin `invoke_llm`, the reference-stub enricher, and `aq vault rebuild-index --with-summaries`.
- **Workflows:** `workflow_stage_resume_handler.py`, `orphan_workflow_recovery.py`, `workflow_pipeline_view.py`
- **Graph layout:** `src/task_graph/layout/` (engine: layering, flow, cost, engine; `driver.py` consumes durable `layout_dirty` marks in the orchestrator cycle and publishes `task_layouts`/`task_layout_cells`/`project_layout_meta` atomically). Commands `graph_layout_rebuild`, `graph_tidy`. On by default (`dashboard.graph_layout.enabled`). API: `src/api/graph_layout.py` (`extent`, `tiles`, `list`, `node`, `locate`, `tidy`, `jobs`); resolution logic in `src/task_graph/layout/view.py`. The persisted layout is the fully expanded geometry; `src/task_graph/layout/compaction.py` derives (never persists) the geometry for a viewer's expanded set — a collapsed container shrinks to one tile and everything after it in reading order reclaims the space, and with nothing collapsed it is the identity on the engine's output (spec §3.5). Spec: `docs/superpowers/specs/2026-09-01-task-graph-spatial-layout-design.md`. Dashboard: `dashboard/src/pages/command-center/layout-v2/` (store, tiles hook, LayoutCanvas) — the only graph view; the `GraphCanvas`/`layout.ts` client-side grid fallback was removed in §10 step 3, so turning the flag off leaves the graph tab empty rather than falling back.
- **Plugins:** `src/plugins/` (base, registry, loader, internal/)
- **Internal plugins:** `src/plugins/internal/` (aq-files, aq-git, aq-notes, aq-vibecop)
- **Messages:** `src/messages/` — `session_lens.py` (`SessionManagerProto`/`SessionLens`; supervisor messaging address `supervisor-<pid>` → runtime name `n-supervisor--<pid>`), `delivery.py` (`MessageDeliveryEngine`; per-`to_kind` delivery policy; parking for stale session rows; transcript-tail fallback). Cascade step in `run_one_cycle` behind `messages.enabled`; delivery to an idle session is the cascade's nudge (`via="nudge"`) — there is no `UserPromptSubmit` hook (removed 2026-08-27), `aq inbox --inject` is an on-demand command only; prime surfaces pending messages via `via="prime"`.
- **Discord (simplified):** one configured channel (`discord.channel_id`), an hourly activity digest and one escalation thread per human decision — nothing else. `src/discord/` is notification-only: `bot.py` (gateway; runs the idempotent `cutover.py` pass in `on_ready` **before** inbound routing goes live), `escalation_transport.py` + `escalation_intake.py` (the only inbound path, calling exactly one command, `escalation_reply`), `embeds.py`, `rate_guard.py`. `slash_commands.py` only *unregisters* the six retired commands; the control views, the notification handler and the 122-command mirror are deleted. Core state is transport-neutral (`src/escalations/`, `escalation_*`/`digest_*` commands, `src/commands/digest_commands.py`). Operator surfaces: `aq digest status|preview`, `aq escalation list|get`, Settings → Messaging. Docs: `docs/guides/discord-migration.md` (runbook), `discord-commands.md`, `discord-replacement-checklist.md`, `escalations.md`. Spec: `docs/superpowers/specs/2026-09-08-discord-simplification-implementation.md` (byte-identical vault mirror at `vault/projects/agent-queue/specs/discord-simplification-implementation.md` — update both together).
- **Subsystems:** `src/runtimes/`, `src/discord/`, `src/git/`, `src/tokens/`, `src/messaging/`
- **Specs:** `docs/specs/` (source of truth — specs first, then code)
- **Design specs:** `docs/specs/design/` (principles, playbooks, memory, self-improvement, coordination, vault, profiles, roadmap)
- **Config:** `~/.agent-queue/config.yaml`
- **Vault:** `~/.agent-queue/vault/` (playbooks, profiles, memory, facts — all markdown)
- **Packages:** `packages/aq-client/` (typed API client)
- **OpenAPI spec:** `openapi.json` is a committed artifact both clients are generated from. `src/api/spec.py` builds it offline (no daemon) — regenerate with `./scripts/regenerate-api-client.sh --offline` and then `./scripts/regenerate-ts-client.sh --from-file` after **any** change to `src/api/models` or a codegen router. `tests/test_api_client_contract.py::test_committed_openapi_json_matches_the_live_app_surface` fails when the committed spec drifts from what `create_app()` serves. The Python generator is pinned exactly (`GENERATOR_VERSION` in the script, `openapi-python-client==` in the `dev` extra, kept in sync by `test_generator_version_pin_agrees_between_the_script_and_the_dev_extra`) — the whole `packages/aq-client/` tree, generated `README.md` included, is a function of the generator version, so an unpinned generator makes regeneration non-idempotent. The generator's post hooks are declared in `scripts/openapi-python-client.yaml` and scoped to the Python package: with the default `ruff format .` a recent ruff also reformats the code blocks inside `README.md`, which made the committed README a function of the box that last regenerated it (`test_generated_client_readme_does_not_depend_on_the_ambient_formatter` guards this). `test_generated_client_boilerplate_matches_what_the_pinned_generator_writes` checks the pin against the tree itself: it regenerates the spec-independent boilerplate from an empty spec (~1s) and fails if the committed files are not byte-for-byte what the pinned generator writes. That test can only run where the pinned generator is installed and skips everywhere else, so the script also records those files' digests in `scripts/aq-client-boilerplate.sha256` and `test_generated_client_boilerplate_matches_the_recorded_digests` checks them from the checkout alone — never hand-edit a file under `packages/aq-client/`, regenerate it.

## Development

```bash
pip install -e ".[dev,cli]"
pip install -e packages/aq-client      # typed API client (generated)
aq test tests/test_orchestrator.py                # focused tests for what you changed (see Testing below)
./run.sh start                         # start daemon
```

- **Swarm end-to-end:** `scripts/e2e-env.sh --reset && scripts/e2e-smoke.sh` drives a real daemon on real PostgreSQL through the pool/claim/formula/hierarchy scenarios via the real CLI, in ~2½ min and with no LLM. See **[docs/guides/e2e-swarm.md](docs/guides/e2e-swarm.md)**; run it after any change to claims, pools, formulas or the task hierarchy.
- Python 3.12+, ruff (line-length 100, py312), pytest-asyncio (auto mode)
- Async-first: use `GitManager` async API (`a`-prefixed), never sync `subprocess.run()` in production
- Commands return `{"success": bool, ...}` dicts
- All state changes go through `CommandHandler` (single entry point for Discord + MCP + CLI)

## Testing — read this before running anything

The suite is **~13,950 tests** (13,816 collected after the default marker deselects). The old per-database replay of 58 alembic migrations is gone — `migrations/versions/` is a squashed baseline plus two revisions, so a fresh test database is cheap and schema setup is no longer the dominant cost. The *suite* still is: a full run is a whole-box, many-minute affair even on 24 cores and **effectively never finishes serially**. Running it casually stalls every agent on the machine.

Rules:
- **Use `aq test`, not bare `pytest`, for anything past a single file.** It takes one of the box's global test slots first, so eight agents testing at once cannot become 200 test processes, and it applies the worker cap and the default marker deselects for you. Everything that is not an `--aq-*` option goes to pytest untouched:
  ```bash
  aq test tests/test_playbook_runner.py          # the file for the module you changed
  aq test tests/test_claim_queries.py tests/test_pools.py
  aq test tests/ -k "schema_setup or run_schema"
  aq test --aq-status                            # who is holding the slots
  aq test --aq-help                              # -h belongs to pytest
  ```
  A `waiting for 1 of 2 test slot(s)` line means the box is busy, not that you are stuck. Exit code 75 means no slot came free — retry, it is not a test failure. Plain `pytest` still works for a single quick file.
- **Never run a bare `pytest` / `pytest tests/` mid-task.** Run only the tests for the code you touch.
- **Never override the worker count upward.** `-n auto` inside a session already resolves to this box's per-session share (`PYTEST_XDIST_AUTO_NUM_WORKERS`, derived from cores ÷ concurrent agents); passing a bigger `-n` bypasses the gating and is what took the box down on 2026-09-01. See [resource gating](docs/guides/resource-gating.md).
- **Find focused tests** (the layout is one file per area, `tests/test_<area>.py`, plus `tests/perf/`, `tests/llm/`, `tests/fixtures/`):
  ```bash
  aq test tests/test_playbook_runner.py            # the file for the module you changed
  aq test tests/test_claim_queries.py tests/test_pools.py    # a few related files
  aq test tests/ -k "schema_setup or run_schema"   # by name, across files
  aq test tests/test_x.py -x                       # stop at first failure while iterating
  aq test --lf                                     # re-run only what failed last time
  pytest --co -q -k <term> | tail -20              # collection only — no slot needed
  ```
- **Skip the slow-by-nature markers** unless the change is about them (real tmux, Milvus, latency budgets). `aq test` applies `-m "not perf and not migration and not slow and not tmux and not integration"` by default; pass your own `-m` (or `--aq-all-markers`) when the change *is* about them.
- **Latency budgets need a quiet box.** Everything in `tests/perf/` is marked `perf`, and every wall-clock budget — there or elsewhere in `tests/` — also takes the `perf_strict` fixture (`tests/conftest.py`) and skips unless `AQ_PERF_STRICT=1`; they measure the machine as much as the query, and an ungated one turns CI's `Tests (default)` arm red on runner load. Run them serially and deliberately: `AQ_PERF_STRICT=1 aq test -m perf -p no:xdist -s tests/perf`. See [resource gating](docs/guides/resource-gating.md).
- **One broader run at the end of a task, not during:** the area suite for what you changed (e.g. `aq test tests/test_playbook*.py tests/test_pipeline*.py`). The whole-repo run is for CI and explicit review gates only.
- Ruff on changed files only: `ruff check <paths>`.

## Database Migrations (Alembic)

Schema is defined in `src/database/tables.py` using SQLAlchemy Core `Table` objects.
Migrations are managed by Alembic in `migrations/`.

**After ANY change to `tables.py`** (add/remove/rename columns, add tables, change constraints):

```bash
# Both commands need AGENT_QUEUE_DB_URL (or sqlalchemy.url in alembic.ini) —
# there is no default, and no URL is a hard error rather than a local file.
export AGENT_QUEUE_DB_URL=postgresql+asyncpg://user:pass@host/db
alembic revision --autogenerate -m "description of change"
# Review the generated file in migrations/versions/
alembic upgrade head  # apply locally
```

- **Never** edit `tables.py` without generating a migration
- **Always** review autogenerated migrations — Alembic can miss renames (it sees drop+add instead)
- PostgreSQL is the only backend: no `batch_alter_table`, no `dialect.name` branches (guarded by `tests/test_sqlite_removal.py`)
- **Always name `CheckConstraint`s** (`name="ck_<table>_<what>"`). Autogenerate matches check constraints by name only, so an unnamed one in `tables.py` is invisible to the comparison and every subsequent autogenerate emits a spurious `drop_constraint` for the auto-named constraint the database actually carries
- `server_default` takes the **bare** value, never a pre-quoted one: `server_default="system"`, not `server_default="'system'"` (SQLAlchemy quotes it again and the DDL becomes `DEFAULT '''system'''`). Booleans use `sa.false()` / `sa.true()`, never `"0"` / `"1"`. Guarded by `tests/test_migration_string_defaults.py` and `tests/test_migration_boolean_defaults.py`
- `src/database/hierarchy_migration.py` is the swarm-work-model hierarchy migration's canonicalisation logic (snapshot → canonicalise → validate → apply), driven by Alembic revisions `a1b2c3d4e5f6` (DDL) and `b2c3d4e5f6a7` (data + partial unique index) and exercised standalone via `aq system db-preflight-hierarchy`
- Test with: `pytest tests/test_database.py -v`

## Never migrate the operator's database

Migrations against the database in `~/.agent-queue/config.yaml` are
**daemon-only**. Inside a worktree slot you must never run `alembic upgrade`,
`alembic stamp`, or `aq start`: on 2026-09-02 a worker did exactly that and
stamped `alembic_version` with an unmerged branch's revision, after which the
daemon refused to boot until an operator hand-wrote a merge revision.

- Your session carries `AQ_DB_SCOPE=worker` and `AQ_DATABASE_URL` /
  `AGENT_QUEUE_DB` set to a sentinel the CLI refuses with an explanation.
  Leave all three alone. Tests build their own temporary databases and are unaffected.
- `run_schema_setup` refuses to migrate the production URL from any scope but
  `daemon`/`operator`, and instead raises **"schema behind code; ask the
  operator to upgrade"**. That is the guard working — report it, do not
  upgrade around it.
- Only an operator, outside a slot, runs `aq db upgrade`. `aq db current` is
  the read-only "am I behind?" answer and is always safe.
- If a database is already stamped with a revision this checkout does not
  have, `aq doctor --check db.alembic_orphan` names the branch and file that
  define it and can repair it with `--fix`.

See [docs/guides/migrations.md](docs/guides/migrations.md) and
`src/database/migration_guard.py`.

## Key Subsystems

- **Self-Improvement Loop:** Reflection engine → knowledge extraction → memory consolidation → prompt builder delivery. Autonomous — no manual intervention needed. See `docs/specs/design/self-improvement.md`.
- **Playbooks:** Markdown-authored DAG workflows compiled to JSON. Replace hooks/rules. Scoped (system/project/agent-type), event-triggered, human-in-the-loop. See `docs/specs/design/playbooks.md`.
- **Workflow Coordination:** Multi-agent pipelines with stage gates, agent affinity, workspace strategies, orphan recovery. See `docs/specs/design/agent-coordination.md`.
- **Workspaces v2:** Typed, normalized workspace model. `workspace_kinds` defines types (e.g. `project-repo`, `package-foo`, `vault`); `workspaces.kind_id` binds instances; `task_workspace_requirements` declares per-task needs. Multi-kind acquisition is all-or-nothing in canonical lock order for deadlock-freedom. Vault is just another auto-attached kind. See `docs/specs/design/workspaces-v2.md`.
- **Memory V2:** Milvus-backed 4-tier knowledge (L0 Identity → L1 Facts → L2 Topic → L3 Search). Semantic search + KV + temporal facts. Multi-scope weighted queries. See `docs/specs/design/memory-plugin.md`.
- **Fleet Metrics:** One sample a second of running agents, sub-agents, tokens/minute, tasks by status, slots, machine load and daemon health; 1s samples for an hour, 1m for 30 days, 1h for a year. Live over the WebSocket, history over `GET /api/metrics/series`. ~0.7% of a core on PostgreSQL. See `docs/specs/design/fleet-metrics.md`.
- **Smart Cascade:** Deterministic promotion cascade each 5s cycle: approvals → resume paused → promote DEFINED → stuck monitoring. Zero LLM overhead.
- **Reflection:** Post-task review with deep/standard/light tiers. Circuit breaker protection. Extracts insights for future agents. See `docs/specs/reflection.md`.
- **Plugins:** 4 internal plugins (files, git, notes, vibecop) + external aq-memory + third-party support. See `docs/specs/plugin-system.md`.
- **MCP Server:** Auto-exposes ~150 CommandHandler commands. See `docs/specs/mcp-server.md`.
- **Vault:** `~/.agent-queue/vault/` — Obsidian-compatible markdown for playbooks, profiles, memory, facts, knowledge bases.
- **Prompt Builder:** 5-layer context assembly pipeline: L0 role → override → L1 facts → L1 guidance → L2 context → identity → tools. Budget-aware per tier (advisory, warn at 2×).

## Detailed Context

See **[profile.md](profile.md)** for full architecture, codebase map, design decisions, and conventions.
